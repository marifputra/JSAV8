const seed = [
  {
    "id": 1001,
    "name": "D. SUPER 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 228000,
    "costBig": 4560000,
    "costPcs": 22800,
    "priceSmall": 229000,
    "priceBig": 4565000,
    "pricePcs": 22800,
    "img": ""
  },
  {
    "id": 1002,
    "name": "D. SUPER 16",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 307000,
    "costBig": 3070000,
    "costPcs": 30700,
    "priceSmall": 308000,
    "priceBig": 3075000,
    "pricePcs": 30700,
    "img": ""
  },
  {
    "id": 1003,
    "name": "D. S. MLD Cola 16",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 296000,
    "costBig": 2960000,
    "costPcs": 29600,
    "priceSmall": 297000,
    "priceBig": 2965000,
    "pricePcs": 29600,
    "img": ""
  },
  {
    "id": 1004,
    "name": "D. SUPER MLD 20",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 366000,
    "costBig": 3660000,
    "costPcs": 36600,
    "priceSmall": 367000,
    "priceBig": 3665000,
    "pricePcs": 36600,
    "img": ""
  },
  {
    "id": 1005,
    "name": "LA LIGHTS 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 220000,
    "costBig": 4400000,
    "costPcs": 22000,
    "priceSmall": 221000,
    "priceBig": 4405000,
    "pricePcs": 22000,
    "img": ""
  },
  {
    "id": 1006,
    "name": "LA LIGHTS 16",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 337000,
    "costBig": 3370000,
    "costPcs": 33700,
    "priceSmall": 338000,
    "priceBig": 3375000,
    "pricePcs": 33700,
    "img": ""
  },
  {
    "id": 1007,
    "name": "LA BOLD 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 219000,
    "costBig": 4380000,
    "costPcs": 21900,
    "priceSmall": 220000,
    "priceBig": 4385000,
    "pricePcs": 21900,
    "img": ""
  },
  {
    "id": 1008,
    "name": "LA BOLD 16",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 292000,
    "costBig": 2920000,
    "costPcs": 29200,
    "priceSmall": 293000,
    "priceBig": 2925000,
    "pricePcs": 29200,
    "img": ""
  },
  {
    "id": 1009,
    "name": "LA BOLD 20",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 373000,
    "costBig": 3730000,
    "costPcs": 37300,
    "priceSmall": 374000,
    "priceBig": 3735000,
    "pricePcs": 37300,
    "img": ""
  },
  {
    "id": 1010,
    "name": "LA MENTHOL 16",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 337000,
    "costBig": 3370000,
    "costPcs": 33700,
    "priceSmall": 338000,
    "priceBig": 3375000,
    "pricePcs": 33700,
    "img": ""
  },
  {
    "id": 1011,
    "name": "LA ICE 16",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 337000,
    "costBig": 3370000,
    "costPcs": 33700,
    "priceSmall": 338000,
    "priceBig": 3375000,
    "pricePcs": 33700,
    "img": ""
  },
  {
    "id": 1012,
    "name": "LA Ice Purple Boost 16",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 337000,
    "costBig": 3370000,
    "costPcs": 33700,
    "priceSmall": 338000,
    "priceBig": 3375000,
    "pricePcs": 33700,
    "img": ""
  },
  {
    "id": 1013,
    "name": "LA Ice Mango Boost 16",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 337000,
    "costBig": 3370000,
    "costPcs": 33700,
    "priceSmall": 338000,
    "priceBig": 3375000,
    "pricePcs": 33700,
    "img": ""
  },
  {
    "id": 1014,
    "name": "D. BLACK 16",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 337000,
    "costBig": 3370000,
    "costPcs": 33700,
    "priceSmall": 338000,
    "priceBig": 3375000,
    "pricePcs": 33700,
    "img": ""
  },
  {
    "id": 1015,
    "name": "D. BLACK CAPP 16",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 337000,
    "costBig": 3370000,
    "costPcs": 33700,
    "priceSmall": 338000,
    "priceBig": 3375000,
    "pricePcs": 33700,
    "img": ""
  },
  {
    "id": 1016,
    "name": "Djarum King 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 218000,
    "costBig": 4360000,
    "costPcs": 21800,
    "priceSmall": 219000,
    "priceBig": 4365000,
    "pricePcs": 21800,
    "img": ""
  },
  {
    "id": 1017,
    "name": "MLD Black Series 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 218000,
    "costBig": 4360000,
    "costPcs": 21800,
    "priceSmall": 219000,
    "priceBig": 4365000,
    "pricePcs": 21800,
    "img": ""
  },
  {
    "id": 1018,
    "name": "D 76 Madu Hitam 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 136000,
    "costBig": 2720000,
    "costPcs": 13600,
    "priceSmall": 137000,
    "priceBig": 2725000,
    "pricePcs": 13600,
    "img": ""
  },
  {
    "id": 1019,
    "name": "D 76 10",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 112000,
    "costBig": 2240000,
    "costPcs": 11200,
    "priceSmall": 113000,
    "priceBig": 2245000,
    "pricePcs": 11200,
    "img": ""
  },
  {
    "id": 1020,
    "name": "D 76 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 154000,
    "costBig": 3080000,
    "costPcs": 15400,
    "priceSmall": 155000,
    "priceBig": 3085000,
    "pricePcs": 15400,
    "img": ""
  },
  {
    "id": 1021,
    "name": "D 76 16",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 210000,
    "costBig": 2100000,
    "costPcs": 21000,
    "priceSmall": 211000,
    "priceBig": 2105000,
    "pricePcs": 21000,
    "img": ""
  },
  {
    "id": 1022,
    "name": "D. Safari 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 125000,
    "costBig": 2500000,
    "costPcs": 12500,
    "priceSmall": 126000,
    "priceBig": 2505000,
    "pricePcs": 12500,
    "img": ""
  },
  {
    "id": 1023,
    "name": "D. CIGARILLOS 6",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 125000,
    "costBig": 2500000,
    "costPcs": 12500,
    "priceSmall": 126000,
    "priceBig": 2505000,
    "pricePcs": 12500,
    "img": ""
  },
  {
    "id": 1024,
    "name": "DS Espresso 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 162000,
    "costBig": 3240000,
    "costPcs": 16200,
    "priceSmall": 163000,
    "priceBig": 3245000,
    "pricePcs": 16200,
    "img": ""
  },
  {
    "id": 1025,
    "name": "DS Espresso Gold 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 176000,
    "costBig": 3520000,
    "costPcs": 17600,
    "priceSmall": 177000,
    "priceBig": 3525000,
    "pricePcs": 17600,
    "img": ""
  },
  {
    "id": 1026,
    "name": "D 76 Mangga 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 145000,
    "costBig": 2900000,
    "costPcs": 14500,
    "priceSmall": 146000,
    "priceBig": 2905000,
    "pricePcs": 14500,
    "img": ""
  },
  {
    "id": 1027,
    "name": "D 76 Royal 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 155000,
    "costBig": 3100000,
    "costPcs": 15500,
    "priceSmall": 156000,
    "priceBig": 3105000,
    "pricePcs": 15500,
    "img": ""
  },
  {
    "id": 1028,
    "name": "D 76 Nanas 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 136000,
    "costBig": 2720000,
    "costPcs": 13600,
    "priceSmall": 137000,
    "priceBig": 2725000,
    "pricePcs": 13600,
    "img": ""
  },
  {
    "id": 1029,
    "name": "D 76 Apel 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 142000,
    "costBig": 2840000,
    "costPcs": 14200,
    "priceSmall": 143000,
    "priceBig": 2845000,
    "pricePcs": 14200,
    "img": ""
  },
  {
    "id": 1030,
    "name": "D 76 Mangga Royal 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 145000,
    "costBig": 2900000,
    "costPcs": 14500,
    "priceSmall": 146000,
    "priceBig": 2905000,
    "pricePcs": 14500,
    "img": ""
  },
  {
    "id": 1031,
    "name": "D 76 Apel Royal 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 145000,
    "costBig": 2900000,
    "costPcs": 14500,
    "priceSmall": 146000,
    "priceBig": 2905000,
    "pricePcs": 14500,
    "img": ""
  },
  {
    "id": 1032,
    "name": "D. Super Kretek Wraps 1",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 159000,
    "costBig": 3180000,
    "costPcs": 15900,
    "priceSmall": 160000,
    "priceBig": 3185000,
    "pricePcs": 15900,
    "img": ""
  },
  {
    "id": 1033,
    "name": "DJARUM D WRAPS 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 134000,
    "costBig": 2680000,
    "costPcs": 13400,
    "priceSmall": 135000,
    "priceBig": 2685000,
    "pricePcs": 13400,
    "img": ""
  },
  {
    "id": 1034,
    "name": "D. Safari Wraps 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 145000,
    "costBig": 2900000,
    "costPcs": 14500,
    "priceSmall": 146000,
    "priceBig": 2905000,
    "pricePcs": 14500,
    "img": ""
  },
  {
    "id": 1035,
    "name": "WARKOP 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 109000,
    "costBig": 2180000,
    "costPcs": 10900,
    "priceSmall": 110000,
    "priceBig": 2185000,
    "pricePcs": 10900,
    "img": ""
  },
  {
    "id": 1036,
    "name": "Warkop Jambu K 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 99000,
    "costBig": 1980000,
    "costPcs": 9900,
    "priceSmall": 100000,
    "priceBig": 1985000,
    "pricePcs": 9900,
    "img": ""
  },
  {
    "id": 1037,
    "name": "SENIOR 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 165000,
    "costBig": 3300000,
    "costPcs": 16500,
    "priceSmall": 166000,
    "priceBig": 3305000,
    "pricePcs": 16500,
    "img": ""
  },
  {
    "id": 1038,
    "name": "Senior Caffe Latte 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 155000,
    "costBig": 3100000,
    "costPcs": 15500,
    "priceSmall": 156000,
    "priceBig": 3105000,
    "pricePcs": 15500,
    "img": ""
  },
  {
    "id": 1039,
    "name": "Ziga K 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 99000,
    "costBig": 1980000,
    "costPcs": 9900,
    "priceSmall": 100000,
    "priceBig": 1985000,
    "pricePcs": 9900,
    "img": ""
  },
  {
    "id": 1040,
    "name": "Ziga 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 165000,
    "costBig": 3300000,
    "costPcs": 16500,
    "priceSmall": 166000,
    "priceBig": 3305000,
    "pricePcs": 16500,
    "img": ""
  },
  {
    "id": 1041,
    "name": "RUDAL 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 115000,
    "costBig": 2300000,
    "costPcs": 11500,
    "priceSmall": 116000,
    "priceBig": 2305000,
    "pricePcs": 11500,
    "img": ""
  },
  {
    "id": 1042,
    "name": "Sergio 20",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 270000,
    "costBig": 2700000,
    "costPcs": 27000,
    "priceSmall": 271000,
    "priceBig": 2705000,
    "pricePcs": 27000,
    "img": ""
  },
  {
    "id": 1043,
    "name": "SLIC KRETEK 16",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 104000,
    "costBig": 1040000,
    "costPcs": 10400,
    "priceSmall": 105000,
    "priceBig": 1045000,
    "pricePcs": 10400,
    "img": ""
  },
  {
    "id": 1044,
    "name": "Raptor 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 165000,
    "costBig": 3300000,
    "costPcs": 16500,
    "priceSmall": 166000,
    "priceBig": 3305000,
    "pricePcs": 16500,
    "img": ""
  },
  {
    "id": 1045,
    "name": "Raptor K 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 89000,
    "costBig": 1780000,
    "costPcs": 8900,
    "priceSmall": 90000,
    "priceBig": 1785000,
    "pricePcs": 8900,
    "img": ""
  },
  {
    "id": 1046,
    "name": "FERRO 16",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 200000,
    "costBig": 2000000,
    "costPcs": 20000,
    "priceSmall": 201000,
    "priceBig": 2005000,
    "pricePcs": 20000,
    "img": ""
  },
  {
    "id": 1047,
    "name": "Shoot K 16",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 87000,
    "costBig": 870000,
    "costPcs": 8700,
    "priceSmall": 88000,
    "priceBig": 875000,
    "pricePcs": 8700,
    "img": ""
  },
  {
    "id": 1048,
    "name": "CHIEF 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 165000,
    "costBig": 3300000,
    "costPcs": 16500,
    "priceSmall": 166000,
    "priceBig": 3305000,
    "pricePcs": 16500,
    "img": ""
  },
  {
    "id": 1049,
    "name": "GEO MILD 16",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 240000,
    "costBig": 2400000,
    "costPcs": 24000,
    "priceSmall": 241000,
    "priceBig": 2405000,
    "pricePcs": 24000,
    "img": ""
  },
  {
    "id": 1050,
    "name": "BLACK SCORPION 20",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 279000,
    "costBig": 2790000,
    "costPcs": 27900,
    "priceSmall": 280000,
    "priceBig": 2795000,
    "pricePcs": 27900,
    "img": ""
  },
  {
    "id": 1051,
    "name": "Viper K 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 99000,
    "costBig": 1980000,
    "costPcs": 9900,
    "priceSmall": 100000,
    "priceBig": 1985000,
    "pricePcs": 9900,
    "img": ""
  },
  {
    "id": 1052,
    "name": "Score Teh Manis K 12",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 20,
    "stockSmall": 0,
    "cost": 79000,
    "costBig": 1580000,
    "costPcs": 7900,
    "priceSmall": 80000,
    "priceBig": 1585000,
    "pricePcs": 7900,
    "img": ""
  },
  {
    "id": 1053,
    "name": "Bit K 16",
    "category": "Rokok",
    "sub": "Rokok Djarum",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 104000,
    "costBig": 1040000,
    "costPcs": 10400,
    "priceSmall": 105000,
    "priceBig": 1045000,
    "pricePcs": 10400,
    "img": ""
  }
];

const dataBarangRokokTambahan = [
  {
    "id": 1054,
    "name": "Gudang Garam Merah",
    "category": "Rokok",
    "sub": "Gudang Garam",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1055,
    "name": "Gudang Garam Djaja",
    "category": "Rokok",
    "sub": "Gudang Garam",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1056,
    "name": "Gudang Garam International",
    "category": "Rokok",
    "sub": "Gudang Garam",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1057,
    "name": "Gudang Garam Filter",
    "category": "Rokok",
    "sub": "Gudang Garam",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1058,
    "name": "Gudang Garam Signature",
    "category": "Rokok",
    "sub": "Gudang Garam",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1059,
    "name": "Gudang Garam Signature Mild",
    "category": "Rokok",
    "sub": "Gudang Garam",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1060,
    "name": "Surya 12",
    "category": "Rokok",
    "sub": "Gudang Garam",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1061,
    "name": "Surya 16",
    "category": "Rokok",
    "sub": "Gudang Garam",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1062,
    "name": "Surya Pro Mild",
    "category": "Rokok",
    "sub": "Gudang Garam",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1063,
    "name": "Surya Professional",
    "category": "Rokok",
    "sub": "Gudang Garam",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1064,
    "name": "GG Mild",
    "category": "Rokok",
    "sub": "Gudang Garam",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1065,
    "name": "GG Move",
    "category": "Rokok",
    "sub": "Gudang Garam",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1066,
    "name": "GG Shiver",
    "category": "Rokok",
    "sub": "Gudang Garam",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1067,
    "name": "GG Bold",
    "category": "Rokok",
    "sub": "Gudang Garam",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1068,
    "name": "Dji Sam Soe 234",
    "category": "Rokok",
    "sub": "Sampoerna",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1069,
    "name": "Dji Sam Soe Super Premium",
    "category": "Rokok",
    "sub": "Sampoerna",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1070,
    "name": "Dji Sam Soe Magnum",
    "category": "Rokok",
    "sub": "Sampoerna",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1071,
    "name": "Dji Sam Soe Magnum Mild",
    "category": "Rokok",
    "sub": "Sampoerna",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1072,
    "name": "Sampoerna Kretek",
    "category": "Rokok",
    "sub": "Sampoerna",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1073,
    "name": "Sampoerna A Mild",
    "category": "Rokok",
    "sub": "Sampoerna",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1074,
    "name": "Sampoerna A Mild 12",
    "category": "Rokok",
    "sub": "Sampoerna",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1075,
    "name": "Sampoerna A Mild 16",
    "category": "Rokok",
    "sub": "Sampoerna",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1076,
    "name": "Sampoerna Avolution",
    "category": "Rokok",
    "sub": "Sampoerna",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1077,
    "name": "Sampoerna U Mild",
    "category": "Rokok",
    "sub": "Sampoerna",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1078,
    "name": "Sampoerna Magnum Filter",
    "category": "Rokok",
    "sub": "Sampoerna",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1079,
    "name": "Sampoerna Magnum Blue",
    "category": "Rokok",
    "sub": "Sampoerna",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1080,
    "name": "Marlboro Merah",
    "category": "Rokok",
    "sub": "Sampoerna / Philip Morris",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1081,
    "name": "Marlboro Gold",
    "category": "Rokok",
    "sub": "Sampoerna / Philip Morris",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1082,
    "name": "Marlboro Black Menthol",
    "category": "Rokok",
    "sub": "Sampoerna / Philip Morris",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1083,
    "name": "Marlboro Filter Black",
    "category": "Rokok",
    "sub": "Sampoerna / Philip Morris",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1084,
    "name": "Bentoel Biru",
    "category": "Rokok",
    "sub": "Bentoel",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1085,
    "name": "Bentoel SJT",
    "category": "Rokok",
    "sub": "Bentoel",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1086,
    "name": "Bentoel Sejati",
    "category": "Rokok",
    "sub": "Bentoel",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1087,
    "name": "Tali Jagat Raya",
    "category": "Rokok",
    "sub": "Bentoel",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1088,
    "name": "Dunhill Mixtures",
    "category": "Rokok",
    "sub": "Bentoel",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1089,
    "name": "Dunhill Fine Cut",
    "category": "Rokok",
    "sub": "Bentoel",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1090,
    "name": "Lucky Strike",
    "category": "Rokok",
    "sub": "Bentoel / BAT",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1091,
    "name": "Ardath",
    "category": "Rokok",
    "sub": "Bentoel / BAT",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1092,
    "name": "Star Mild",
    "category": "Rokok",
    "sub": "Bentoel",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1093,
    "name": "Neo Mild",
    "category": "Rokok",
    "sub": "Bentoel",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1094,
    "name": "Wismilak Diplomat",
    "category": "Rokok",
    "sub": "Wismilak",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1095,
    "name": "Wismilak Diplomat Blaze Filter",
    "category": "Rokok",
    "sub": "Wismilak",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1096,
    "name": "Diplomat Mild",
    "category": "Rokok",
    "sub": "Wismilak",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1097,
    "name": "Diplomat Mild Menthol",
    "category": "Rokok",
    "sub": "Wismilak",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1098,
    "name": "Diplomat Mild Berry Spark",
    "category": "Rokok",
    "sub": "Wismilak",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1099,
    "name": "Diplomat Mild Guava Spark",
    "category": "Rokok",
    "sub": "Wismilak",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1100,
    "name": "Diplomat Mild Lychee Spark",
    "category": "Rokok",
    "sub": "Wismilak",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1101,
    "name": "Diplomat Mild Mango Spark",
    "category": "Rokok",
    "sub": "Wismilak",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1102,
    "name": "Diplomat Evo",
    "category": "Rokok",
    "sub": "Wismilak",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1103,
    "name": "Diplomat Evo Mango Squizz",
    "category": "Rokok",
    "sub": "Wismilak",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1104,
    "name": "Diplomat Evo Berry Squizz",
    "category": "Rokok",
    "sub": "Wismilak",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1105,
    "name": "Diplomat Evo Lychee Squizz",
    "category": "Rokok",
    "sub": "Wismilak",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1106,
    "name": "Diplomat Evo Guava Squizz",
    "category": "Rokok",
    "sub": "Wismilak",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1107,
    "name": "Galan Kretek",
    "category": "Rokok",
    "sub": "Wismilak",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1108,
    "name": "Galan Prima",
    "category": "Rokok",
    "sub": "Wismilak",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1109,
    "name": "Galan Kretek Edisi Kawan",
    "category": "Rokok",
    "sub": "Wismilak",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1110,
    "name": "Wismilak Spesial",
    "category": "Rokok",
    "sub": "Wismilak",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1111,
    "name": "Wismilak Slim",
    "category": "Rokok",
    "sub": "Wismilak",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1112,
    "name": "Wismilak Satya",
    "category": "Rokok",
    "sub": "Wismilak",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1113,
    "name": "Wismilak Satya Wraps",
    "category": "Rokok",
    "sub": "Wismilak",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1114,
    "name": "Wismilak ARJA",
    "category": "Rokok",
    "sub": "Wismilak",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1115,
    "name": "ARJA Mangga Arumanis",
    "category": "Rokok",
    "sub": "Wismilak",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1116,
    "name": "ARJA Anggur",
    "category": "Rokok",
    "sub": "Wismilak",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1117,
    "name": "ARJA Leci",
    "category": "Rokok",
    "sub": "Wismilak",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1118,
    "name": "Gudang Baru International",
    "category": "Rokok",
    "sub": "Gudang Baru",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1119,
    "name": "Gudang Baru Origin",
    "category": "Rokok",
    "sub": "Gudang Baru",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1120,
    "name": "Gudang Baru Filter",
    "category": "Rokok",
    "sub": "Gudang Baru",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  },
  {
    "id": 1121,
    "name": "Andalan Filter",
    "category": "Rokok",
    "sub": "Gudang Baru",
    "bigUnit": "bal",
    "smallUnit": "slop",
    "contentPerBig": 10,
    "stockSmall": 0,
    "cost": 0,
    "costBig": 0,
    "costPcs": 0,
    "priceSmall": 0,
    "priceBig": 0,
    "pricePcs": 0,
    "img": ""
  }
];

const dataBarangObatRaw = `
1|Alkohol 70%|110.500|4.800|1 pak = 24 botol
2|Antimo Dewasa|290.000|4.200|1 pak = 72 strip
3|Antimo Kids|12.500|-|1 pak = 10 sachet
4|Balpirik Merah|80.000|6.800|1 pak = 12 dus
5|Balsem Geliga 10 gr|51.500|4.500|1 pak = 12 dus
6|Balsem Geliga 20 gr|93.000|8.000|1 pak = 12 dus
7|Balsem Lang 10 gr|51.500|4.500|1 pak = 12 dus
8|Balsem Lang 20 gr|93.000|8.000|1 pak = 12 dus
9|Betadine (1 dus = 6 botol)|21.500|-|1 karton = 24 pak
10|Biogesic|45.000|2.000|1 pak = 25 strip
11|Bodrex|75.000|6.500|1 pak = 12 dus
12|Bodrex Extra|42.000|1.800|1 pak = 25 blister
13|Bodrex Flu Batuk|37.000|1.600|1 pak = 25 strip
14|Bodrex Migra|42.000|1.800|1 pak = 25 strip
15|Bodrexin|39.000|3.400|1 pak = 12 dus
16|CTM|17.000|800|1 pak = 24 dus
17|Decolgen|43.500|1.800|1 pak = 25 strip
18|Decolsin|60.500|2.500|1 pak = 25 strip
19|Dettol Antiseptic Cair 50 ml|-|6.100|-
20|Diapet|46.500|2.000|1 pak = 25 strip
21|Entrostop Anak|31.000|10.500|1 pak = 3 dus
22|Entrostop Dewasa|67.000|11.400|1 pak = 6 dus
23|Enervon-C|105.000|4.300|1 pak = 25 strip
24|Fatigon Biasa|44.500|3.200|1 pak = 15 strip
25|Fatigon Spirit|33.500|-|1 pak = 6 strip
26|GPU 30 ml|96.500|8.200|1 pak = 12 dus
27|GPU 60 ml|166.000|14.000|1 pak = 12 dus
28|Hansaplast Box 100|24.000|-|1 karton = 100 pak
29|Hemaviton Action|56.500|5.800|1 pak = 10 blister
30|Hemaviton Stamina Plus|54.000|5.500|1 pak = 10 blister
31|Insto|138.000|11.700|1 pak = 12 dus
32|Inza|46.000|2.000|1 pak = 25 strip
33|Inzana|19.500|900|1 pak = 25 strip
34|Kalpanax Cair|33.200|2.900|1 pak = 12 dus
35|Komix Dewasa|34.000|-|-
36|Komix Kids|30.000|10.200|1 pak = 3 dus
37|Konidin|83.000|1.800|1 pak = 50 strip
38|Koyo Cabe|160.000|8.200|1 pak = 20 strip
39|Mextril|40.000|1.700|1 pak = 25 strip
40|Minyak Angin Fresh Care|116.500|10.000|1 pak = 12 botol
41|Minyak Angin Safe Care|330.500|14.000|1 karton = 12 pak
42|Minyak Kapak 3 ml|55.500|4.800|1 pak = 12 dus
43|Minyak Kapak 5 ml|96.000|8.200|1 pak = 12 dus
44|Minyak Kapak 10 ml|150.000|12.700|1 pak = 12 dus
45|Minyak Kapak 14 ml|205.000|17.300|1 pak = 12 dus
46|Minyak Kapak 28 ml|347.500|29.200|1 pak = 12 dus
47|Minyak Tawon CC|368.000|15.600|1 pak = 24 botol
48|Minyak Tawon DD|498.000|21.000|1 pak = 24 botol
49|Minyak Tawon EE|405.000|34.000|1 pak = 12 botol
50|Minyak Telon Anak 60 ml|78.500|-|1 karton = 20 pak
51|Minyak Telon Anak 100 ml|110.000|-|1 karton = 12 pak
52|Mixagrip|44.500|1.900|1 pak = 25 strip
53|Mylanta Tablet|60.000|6.200|1 pak = 10 blister
54|Napacin|100.000|2.100|1 pak = 50 strip
55|Neo Rheumacyl|172.500|7.300|1 pak = 24 dus
56|Neozep|52.000|2.300|1 pak = 25 strip
57|Oskadon|78.000|1.700|1 pak = 50 strip
58|Panadol Biru|74.000|7.600|1 pak = 10 blister
59|Panadol Hijau|95.000|9.700|1 pak = 10 blister
60|Panadol Merah|80.000|8.200|1 pak = 10 blister
61|Paracetamol|17.500|1.900|1 pak = 10 blister
62|Paramex Flu Batuk|44.500|1.900|1 pak = 25 strip
63|Paramex Nyeri Otot|45.000|2.000|1 pak = 25 strip
64|Paramex Sakit Kepala|91.000|2.000|1 pak = 50 strip
65|Poldan Mig|59.500|2.500|1 pak = 25 strip
66|Procold|60.000|2.600|1 pak = 24 blister
67|Promag Tablet|68.000|17.200|1 pak = 4 dus
68|Puyer 16|57.500|6.000|1 pak = 10 lembar
69|Rivanol|56.500|2.600|1 pak = 24 botol
70|Rohto|122.000|10.400|1 pak = 12 dus
71|Salep Sriti|133.000|5.500|1 pak = 25 dus
72|Salonpas|63.000|6.400|1 pak = 10 strip
73|Saridon|97.000|3.400|1 pak = 30 blister
74|Ultracap|53.500|2.200|1 pak = 25 strip
75|Ultraflu|63.000|2.700|1 pak = 25 strip
76|Ultrasiline|87.500|-|1 pak = 12 tube
77|Vicks Inhaler|76.000|-|1 hanger = 6 tube
78|Vicks Vaporub 10 gr|71.000|-|1 pak = 12 biji
79|Visine|165.500|14.000|1 pak = 12 dus
80|Vitacimin|68.500|7.100|1 pak = 10 iket
81|Xon-Ce|59.500|30.000|1 pak = 2 bungkus
`.trim();

function parseHargaObat(value) {
  const text = String(value || '').trim();
  if (!text || text === '-') return 0;
  return Number(text.replace(/\./g, '')) || 0;
}

function parseKemasanObat(value) {
  const match = String(value || '').match(/1\s+(\w+)\s*=\s*(\d+)\s+(\w+)/i);
  if (!match) return { bigUnit: 'pak', smallUnit: 'pcs', contentPerBig: 1 };
  return {
    bigUnit: match[1].toLowerCase(),
    smallUnit: match[3].toLowerCase(),
    contentPerBig: Number(match[2]) || 1
  };
}

function subKategoriObat(name) {
  if (/minyak|fresh care|safe care|gpu/i.test(name)) return 'Minyak Angin';
  return /enervon|fatigon|hemaviton|vitacimin|xon-ce/i.test(name) ? 'Vitamin' : 'Obat Umum';
}

const dataBarangObatTambahan = dataBarangObatRaw.split('\n').map(line => {
  const [no, name, perPak, eceran, banyaknya] = line.split('|');
  const kemasan = parseKemasanObat(banyaknya);
  return {
    id: 5000 + Number(no),
    name,
    category: 'Obat-obatan',
    sub: subKategoriObat(name),
    bigUnit: kemasan.bigUnit,
    smallUnit: kemasan.smallUnit,
    contentPerBig: kemasan.contentPerBig,
    stockSmall: 0,
    cost: 0,
    costBig: 0,
    costPcs: 0,
    priceSmall: parseHargaObat(eceran),
    priceBig: parseHargaObat(perPak),
    pricePcs: 0,
    img: ''
  };
});

const defaultCategories = [
  { name: 'Snack', subs: ['Snack Karton', 'Biskuit', 'Keripik'] },
  { name: 'Sembako', subs: ['Gula', 'Beras', 'Minyak', 'Tepung'] },
  { name: 'Rokok', subs: ['Rokok Djarum', 'Rokok Filter', 'Rokok Kretek'] },
  { name: 'Obat-obatan', subs: ['Obat Umum', 'Vitamin', 'Minyak Angin'] }
];

const rp = n => 'Rp ' + Number(n || 0).toLocaleString('id-ID');
const n = v => Number(v || 0);

function normalizeCategoryName(name = '') {
  const clean = String(name || '').trim();
  const key = clean.toLowerCase().replace(/[\s-]+/g, ' ');
  if (key === 'obat' || key === 'obat obatan' || key === 'obat obat an') return 'Obat-obatan';
  return clean || 'Umum';
}

function normalizeSubName(category = '', sub = '') {
  const categoryName = normalizeCategoryName(category);
  const clean = String(sub || '').trim();
  const key = clean.toLowerCase().replace(/[\s-]+/g, ' ');
  if (categoryName === 'Obat-obatan') {
    if (key === 'vitamin') return 'Vitamin';
    if (key === 'minyak angin') return 'Minyak Angin';
    return 'Obat Umum';
  }
  return clean || 'Umum';
}

const productImagePalettes = {
  'Minuman': ['d8f5ff', '123243'],
  'Minuman Instant': ['d8f5ff', '123243'],
  'Makanan': ['fff2cf', '4a2f00'],
  'Makanan Instant': ['fff2cf', '4a2f00'],
  'Snack': ['ffe4e6', '7f1d1d'],
  'Sembako': ['e8f5e9', '163b20'],
  'Bumbu': ['fff0db', '5b2d08'],
  'Kecap': ['fff0db', '5b2d08'],
  'Saos': ['ffe1df', '7c1b16'],
  'Tepung': ['f4f0e8', '3f3529'],
  'Bahan Kue': ['f4f0e8', '3f3529'],
  'SUSU': ['edf7ff', '173a5e'],
  'Rokok': ['e9eef2', '172a35'],
  'Obat-obatan': ['eef7ff', '163a5f'],
  'Kosmetik': ['fde7f3', '6b1b46'],
  'Detergen': ['e8f7ff', '17475f'],
  'Peralatan': ['eef2f7', '263443'],
  'Popok': ['f0e9ff', '3f2366'],
  'Permen': ['ffe8f0', '7a1740'],
  'Roti': ['fff0dc', '5a3211'],
  'Dll': ['eefbff', '123243'],
  'Umum': ['eefbff', '123243']
};

function autoProductImageUrl(product = {}) {
  const category = normalizeCategoryName(product.category || 'Umum');
  const brand = String(product.brand || product.sub || getMerek(product.name || '') || category || 'Produk').trim();
  const title = encodeURIComponent((brand || category).slice(0, 28));
  const [bg, fg] = productImagePalettes[category] || productImagePalettes.Umum;
  return `https://placehold.co/600x400/${bg}/${fg}?text=${title}`;
}

function getProductImageUrl(product = {}) {
  return product.img || autoProductImageUrl(product);
}

function getMerek(name = '') {
  const s = String(name).toLowerCase();

  if (s.includes('d. super') || s.includes('djarum super') || s.includes('super mld')) return 'Djarum Super';
  if (s.includes('d 76') || s.includes('d. safari') || s.includes('djarum d') || s.includes('d. cigarillos')) return 'Djarum 76';
  if (s.includes('la ')) return 'LA';
  if (s.includes('mld')) return 'MLD';
  if (s.includes('warkop')) return 'Warkop';
  if (s.includes('ziga')) return 'Ziga';
  if (s.includes('senior')) return 'Senior';
  if (s.includes('raptor')) return 'Raptor';
  if (s.includes('surya')) return 'Surya';
  if (s.includes('inter')) return 'Surya';
  if (s.includes('gudang garam') || s.includes('gg ')) return 'Gudang Garam';
  if (s.includes('dji sam soe')) return 'Dji Sam Soe';
  if (s.includes('sampoerna') || s.includes('a mild') || s.includes('magnum')) return 'Sampoerna';
  if (s.includes('marlboro')) return 'Marlboro';
  if (s.includes('bentoel') || s.includes('tali jagat')) return 'Bentoel';
  if (s.includes('dunhill')) return 'Dunhill';
  if (s.includes('lucky strike')) return 'Lucky Strike';
  if (s.includes('ardath')) return 'Ardath';
  if (s.includes('star mild')) return 'Star Mild';
  if (s.includes('neo mild')) return 'Neo Mild';
  if (s.includes('wismilak')) return 'Wismilak';
  if (s.includes('diplomat')) return 'Diplomat';
  if (s.includes('galan')) return 'Galan';
  if (s.includes('arja')) return 'ARJA';
  if (s.includes('gudang baru') || s.includes('andalan')) return 'Gudang Baru';
  if (s.includes('geo')) return 'Geo Mild';
  if (s.includes('chief')) return 'Chief';
  if (s.includes('ferro')) return 'Ferro';
  if (s.includes('black')) return 'Black';
  return 'Lainnya';
}

function makeSearchText(p) {
  const brand = p.brand || getMerek(p.name);
  let text = `${p.name} ${p.category} ${p.sub} ${brand}`.toLowerCase();

  if (text.includes('d.')) text += ' djarum';
  if (text.includes('d ')) text += ' djarum';
  if (text.includes('d 76')) text += ' djarum 76 tujuh enam';
  if (text.includes('super')) text += ' djarum super';
  if (text.includes('la')) text += ' la lights bold ice';
  if (text.includes('slop')) text += ' stang slof';
  if (text.includes('bungkus')) text += ' pcs pack';
  if (text.includes('surya')) text += ' gudang garam inter';

  return text;
}

let activeMerek = 'Semua';
let stockView = 'alert';
let stockAlertShown = false;
const PRIMARY_CATALOG_CATEGORIES = ['Semua', 'Rokok', 'Obat-obatan', 'Bahan Pokok'];
const BAHAN_POKOK_CATEGORIES = new Set([
  'Sembako', 'Bahan Pokok', 'Bahan Kue', 'Bumbu', 'Garam', 'Kacang',
  'Kecap', 'Makanan', 'Makanan Instant', 'Minyak', 'Penyedap',
  'Saos', 'Tepung', 'SUSU'
]);
let catalogFilterCategories = ['Semua'];
let catalogFilterBrands = ['Semua'];


const SERVER_MODE = location.protocol === 'http:' || location.protocol === 'https:';
const API_BASE = (window.JSA_API_BASE || '').replace(/\/$/, '');
const IS_ADMIN_PAGE = location.pathname.includes('admin');
const STATE_READ_ENDPOINT = IS_ADMIN_PAGE ? '/api/state' : '/api/public-state';
let syncingState = false;
let serverSyncAvailable = false;
let adminRefreshTimer = null;
let adminRefreshBusy = false;
const adminRefreshMs = 8000;

function getAdminToken() {
  return localStorage.getItem('jsa_admin_token') || window.JSA_ADMIN_TOKEN || '';
}

function apiHeaders(extra = {}) {
  const adminToken = getAdminToken();
  return {
    ...extra,
    ...(adminToken ? { 'X-Admin-Token': adminToken, Authorization: `Bearer ${adminToken}` } : {})
  };
}

const getCategories = () => normalizeCategories(JSON.parse(localStorage.getItem('jsa_categories') || 'null') || defaultCategories);
function setCategories(categories) {
  const normalized = normalizeCategories(categories);
  localStorage.setItem('jsa_categories', JSON.stringify(normalized));
  saveStateToServerSoon({ categories: normalized });
}

function normalizeCategories(categories) {
  const byName = new Map();
  (categories || []).forEach(category => {
    const name = normalizeCategoryName(category.name);
    const subs = Array.isArray(category.subs) && category.subs.length ? category.subs : ['Umum'];
    if (!byName.has(name)) {
      byName.set(name, { name, subs: [] });
    }
    const current = byName.get(name);
    current.subs = [...new Set([...current.subs, ...subs.map(sub => normalizeSubName(name, sub))])];
  });
  return [...byName.values()];
}

function normalizeProduct(p) {
  const contentPerBig = n(p.contentPerBig || p.conversion || 1) || 1;
  const stockSmall = p.stockSmall !== undefined ? n(p.stockSmall) : n(p.stockBase || p.stock || 0);

  return {
    ...p,
    category: normalizeCategoryName(p.category),
    sub: normalizeSubName(p.category, p.sub),
    bigUnit: p.bigUnit || p.buyUnit || 'karton',
    smallUnit: p.smallUnit || p.baseUnit || 'pcs',
    contentPerBig,
    stockSmall,
    cost: n(p.cost),
    priceSmall: n(p.priceSmall || p.retail || p.wholesale),
    priceBig: n(p.priceBig || p.retailBig || p.wholesaleBig || ((p.priceSmall || p.retail || p.wholesale || 0) * contentPerBig)),
    brand: p.brand || getMerek(p.name),
    thresholdSmall: Number(p.thresholdSmall ?? 5),
    priceMember1Small: Number(p.priceMember1Small || 0),
    priceMember1Big: Number(p.priceMember1Big || 0),
    priceMember2Small: Number(p.priceMember2Small || 0),
    priceMember2Big: Number(p.priceMember2Big || 0)
  };
}

const getProducts = () => (JSON.parse(localStorage.getItem('jsa_products') || 'null') || seed).map(normalizeProduct);
function setProducts(products) {
  const normalized = products.map(normalizeProduct);
  localStorage.setItem('jsa_products', JSON.stringify(normalized));
  saveStateToServerSoon({ products: normalized });
}

function syncCategoriesWithProducts() {
  const categories = getCategories();
  const byName = new Map(categories.map(category => [category.name, { ...category, subs: [...category.subs] }]));
  const productSubs = new Map();

  getProducts().forEach(product => {
    const categoryName = normalizeCategoryName(product.category);
    const subName = normalizeSubName(categoryName, product.sub);
    if (!productSubs.has(categoryName)) productSubs.set(categoryName, new Set());
    productSubs.get(categoryName).add(subName);
    if (!byName.has(categoryName)) {
      byName.set(categoryName, { name: categoryName, subs: [] });
    }
  });

  productSubs.forEach((subs, categoryName) => {
    byName.set(categoryName, {
      name: categoryName,
      subs: [...subs]
    });
  });

  const next = normalizeCategories([...byName.values()]);
  const oldText = JSON.stringify(categories);
  const nextText = JSON.stringify(next);
  if (oldText !== nextText) {
    setCategories(next);
  }
}

function syncStoredProductsWithNormalization() {
  const raw = JSON.parse(localStorage.getItem('jsa_products') || 'null');
  if (!Array.isArray(raw)) return;
  const normalized = raw.map(normalizeProduct);
  if (JSON.stringify(raw) !== JSON.stringify(normalized)) {
    setProducts(normalized);
  }
}

function ensureDataBarangRokokTambahan() {
  const products = getProducts();
  const ids = new Set(products.map(p => String(p.id)));
  const names = new Set(products.map(p => String(p.name || '').trim().toLowerCase()));
  let changed = false;

  const stokBarangTambahan = Array.isArray(window.JSA_STOK_BARANG_TAMBAHAN) ? window.JSA_STOK_BARANG_TAMBAHAN : [];

  [...dataBarangRokokTambahan, ...dataBarangObatTambahan, ...stokBarangTambahan].forEach(item => {
    const nameKey = String(item.name || '').trim().toLowerCase();
    if (ids.has(String(item.id)) || names.has(nameKey)) return;

    products.push(normalizeProduct({ ...item }));
    ids.add(String(item.id));
    names.add(nameKey);
    changed = true;
  });

  if (changed) setProducts(products);
}

const getOrders = () => JSON.parse(localStorage.getItem('jsa_orders') || '[]');
function setOrders(orders) {
  localStorage.setItem('jsa_orders', JSON.stringify(orders));
  saveStateToServerSoon({ orders });
}

const getMembers = () => JSON.parse(localStorage.getItem('jsa_members') || '[]');
function setMembers(members) {
  localStorage.setItem('jsa_members', JSON.stringify(members));
  saveStateToServerSoon({ members });
}

function buyerLevelLabel(level) {
  return level || 'MEMBER';
}

function normalizePhone(phone) {
  return String(phone || '').replace(/[^\d+]/g, '');
}

function getCheckoutProfile() {
  return JSON.parse(localStorage.getItem('jsa_checkout_profile') || 'null') || {
    name: '',
    phone: ''
  };
}

function saveCheckoutProfile() {
  const profile = {
    name: byId('buyerName')?.value.trim() || '',
    phone: normalizePhone(byId('buyerPhone')?.value || '')
  };
  localStorage.setItem('jsa_checkout_profile', JSON.stringify(profile));
  return profile;
}

function getMember() {
  return JSON.parse(localStorage.getItem('jsa_member') || 'null');
}
const byId = id => document.getElementById(id);

function currentState() {
  return {
    products: getProducts(),
    categories: getCategories(),
    orders: getOrders(),
    members: getMembers()
  };
}

function applyServerState(state) {
  if (Array.isArray(state.products) && (IS_ADMIN_PAGE || state.products.length)) localStorage.setItem('jsa_products', JSON.stringify(state.products.map(normalizeProduct)));
  if (Array.isArray(state.categories)) localStorage.setItem('jsa_categories', JSON.stringify(state.categories));
  if (Array.isArray(state.orders)) localStorage.setItem('jsa_orders', JSON.stringify(state.orders));
  if (Array.isArray(state.members)) localStorage.setItem('jsa_members', JSON.stringify(state.members));
}

let saveTimer = null;
let pendingStatePatch = {};
function saveStateToServerSoon(patch = {}) {
  if (!SERVER_MODE || syncingState) return;
  pendingStatePatch = { ...pendingStatePatch, ...patch };
  clearTimeout(saveTimer);
  saveTimer = setTimeout(saveStateToServer, 250);
}

async function saveStateToServer(patch = null) {
  if (!SERVER_MODE || syncingState) return;
  const payload = patch || pendingStatePatch;
  pendingStatePatch = {};
  if (!Object.keys(payload).length) return;
  try {
    await fetch(API_BASE + '/api/state', {
      method: 'POST',
      headers: apiHeaders({ 'Content-Type': 'application/json' }),
      body: JSON.stringify(payload)
    });
  } catch (err) {
    console.warn('Gagal sinkron ke server lokal:', err);
  }
}

async function loadStateFromServer() {
  if (!SERVER_MODE) return;
  try {
    const res = await fetch(API_BASE + STATE_READ_ENDPOINT, {
      cache: 'no-store',
      headers: apiHeaders()
    });
    const state = await res.json();
    syncingState = true;
    if (Array.isArray(state.products) && Array.isArray(state.categories)) {
      applyServerState(state);
      serverSyncAvailable = true;
    } else {
      await fetch(API_BASE + '/api/state', {
        method: 'POST',
        headers: apiHeaders({ 'Content-Type': 'application/json' }),
        body: JSON.stringify(currentState())
      });
      serverSyncAvailable = true;
    }
  } catch (err) {
    serverSyncAvailable = false;
    console.warn('Mode server lokal tidak aktif, memakai data browser:', err);
  } finally {
    syncingState = false;
  }
}

function adminDashboardVisible() {
  return IS_ADMIN_PAGE && localStorage.getItem('jsa_admin') === '1' && !document.getElementById('dashboard')?.classList.contains('hidden');
}

function adminIsEditing() {
  const page = localStorage.getItem('jsa_admin_page') || 'kategori';
  if (page === 'tambah' || page === 'backup') return true;
  if (page === 'barang' && openProductId) return true;

  const el = document.activeElement;
  if (!el) return false;
  const tag = el.tagName;
  return ['INPUT', 'TEXTAREA', 'SELECT'].includes(tag) && !el.readOnly;
}

function renderCurrentAdminPage() {
  const page = localStorage.getItem('jsa_admin_page') || 'kategori';
  renderStats();
  updateStockBadge();

  if (page === 'stok') renderStockReport();
  if (page === 'member') renderOrders();
  if (page === 'barang') renderAdmin();
  if (page === 'kategori') {
    renderCategoryAdmin();
    refreshCategoryOptions();
  }
  if (page === 'akun') renderMembers();
}

async function refreshAdminFromServer(force = false) {
  if (!adminDashboardVisible() || adminRefreshBusy) return;
  if (!force && adminIsEditing()) return;
  if (!force && !serverSyncAvailable) return;

  adminRefreshBusy = true;
  try {
    await loadStateFromServer();
    if (serverSyncAvailable) renderCurrentAdminPage();
  } finally {
    adminRefreshBusy = false;
  }
}

function startAdminAutoRefresh() {
  if (adminRefreshTimer || !IS_ADMIN_PAGE || !SERVER_MODE) return;
  adminRefreshTimer = setInterval(() => refreshAdminFromServer(false), adminRefreshMs);
}

function stopAdminAutoRefresh() {
  if (!adminRefreshTimer) return;
  clearInterval(adminRefreshTimer);
  adminRefreshTimer = null;
}

const memberAccounts = [
  { username:'member1', password:'member123', name:'Member 1', phone:'0811111111', level:'MEMBER 1' },
  { username:'member2', password:'member123', name:'Member 2', phone:'0822222222', level:'MEMBER 2' },
  { username:'member3', password:'member123', name:'Member 3', phone:'0833333333', level:'MEMBER 3' }
];

function getMemberPrice(p, saleMode){
  const member = getMember();
  const normalPrice = saleMode === 'big' ? Number(p.priceBig || 0) : Number(p.priceSmall || 0);

  if(!member) return normalPrice;

  if(member.level === 'MEMBER 1'){
    const manualPrice = saleMode === 'big'
      ? Number(p.priceMember1Big || 0)
      : Number(p.priceMember1Small || 0);
    return manualPrice > 0 ? manualPrice : normalPrice;
  }

  if(member.level === 'MEMBER 2'){
    const manualPrice = saleMode === 'big'
      ? Number(p.priceMember2Big || 0)
      : Number(p.priceMember2Small || 0);
    return manualPrice > 0 ? manualPrice : normalPrice;
  }

  // Member 3 memakai harga normal
  return normalPrice;
}

function getMemberPriceLabel(){
  const m = getMember();
  if(!m) return 'Harga umum';
  return buyerLevelLabel(m.level);
}

async function authenticateMember(username, password) {
  if (SERVER_MODE) {
    try {
      const res = await fetch(API_BASE + '/api/member-login', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ username, password })
      });
      const result = await res.json();
      if (res.ok && result.ok && result.member) return result.member;
      throw new Error(result.error || 'Username atau password member salah.');
    } catch (err) {
      console.warn('Login member server gagal, memakai akun demo lokal:', err);
    }
  }

  const account = memberAccounts.find(m => m.username === username && m.password === password);
  if(!account) throw new Error('Username atau password member salah.');
  return {
    id: account.username,
    username: account.username,
    name: account.name,
    phone: account.phone,
    level: account.level,
    joinedAt: new Date().toLocaleString('id-ID')
  };
}


let cart = JSON.parse(localStorage.getItem('jsa_cart') || '[]');
let cartOpen = localStorage.getItem('jsa_cart_open') === '1';
let active = 'Semua';
let openSubKey = localStorage.getItem('jsa_open_sub') || '';
let adminPage = 1;
const adminPerPage = 30;
let productPage = 1;
const productPerPage = 60;
let openProductId = localStorage.getItem('jsa_open_product') || '';
let openProductGroups = JSON.parse(localStorage.getItem('jsa_open_product_groups') || '{}');
let publicCatalogMeta = null;
let productFetchSeq = 0;

function setActiveCategory(encoded) {
  active = decodeURIComponent(encoded);
  activeMerek = 'Semua';
  productPage = 1;
  renderProducts();
}

function setActiveMerek(encoded) {
  activeMerek = decodeURIComponent(encoded);
  productPage = 1;
  renderProducts();
}

function isBahanPokokCategory(category) {
  return BAHAN_POKOK_CATEGORIES.has(normalizeCategoryName(category));
}

function closeCatalogFilterPopup() {
  const modal = document.getElementById('catalogFilterModal');
  if (modal) modal.classList.add('hidden');
}

function openCatalogFilterPopup() {
  const modal = document.getElementById('catalogFilterModal');
  const box = document.getElementById('catalogFilterBody');
  if (!modal || !box) return;
  const categories = [...new Set(['Semua', ...catalogFilterCategories.filter(Boolean), 'Rokok', 'Obat-obatan', 'Bahan Pokok'])];
  const brands = [...new Set(['Semua', ...catalogFilterBrands.filter(Boolean)])];
  box.innerHTML = `
    <div class="filter-popup-section">
      <h3>Kategori</h3>
      <div class="popup-chip-grid">
        ${categories.map(c => `<button class="chip ${active === c ? 'active' : ''}" onclick="setActiveCategory('${encodeURIComponent(c)}');closeCatalogFilterPopup()">${escapeHtml(c)}</button>`).join('')}
      </div>
    </div>
    <div class="filter-popup-section">
      <h3>Merek</h3>
      <div class="popup-chip-grid">
        ${brands.map(b => `<button class="chip ${activeMerek === b ? 'active' : ''}" onclick="setActiveMerek('${encodeURIComponent(b)}');closeCatalogFilterPopup()">${escapeHtml(b)}</button>`).join('')}
      </div>
    </div>
  `;
  modal.classList.remove('hidden');
}

function renderCatalogFilters(categories = ['Semua'], brands = ['Semua']) {
  catalogFilterCategories = categories;
  catalogFilterBrands = brands;
  const filterBox = document.getElementById('filters');
  if (!filterBox) return;
  const visible = PRIMARY_CATALOG_CATEGORIES;
  const moreActive = !visible.includes(active) || activeMerek !== 'Semua';
  filterBox.innerHTML = `
    ${visible.map(c => `<button class="chip ${active === c ? 'active' : ''}" onclick="setActiveCategory('${encodeURIComponent(c)}')">${escapeHtml(c)}</button>`).join('')}
    <button class="chip filter-more ${moreActive ? 'active' : ''}" onclick="openCatalogFilterPopup()">Filter lainnya</button>
  `;
  const brandBox = document.getElementById('brandFilters');
  if (brandBox) {
    brandBox.innerHTML = activeMerek !== 'Semua'
      ? `<button class="chip active" onclick="setActiveMerek('Semua')">Merek: ${escapeHtml(activeMerek)} &times;</button>`
      : '';
  }
  ensureCatalogFilterModal();
}

function ensureCatalogFilterModal() {
  if (document.getElementById('catalogFilterModal')) return;
  const wrap = document.createElement('div');
  wrap.id = 'catalogFilterModal';
  wrap.className = 'modal hidden';
  wrap.innerHTML = `
    <div class="modal-box filter-modal-box">
      <div class="modal-head">
        <h2>Filter Barang</h2>
        <button class="btn secondary" onclick="closeCatalogFilterPopup()">Tutup</button>
      </div>
      <div id="catalogFilterBody"></div>
    </div>
  `;
  wrap.addEventListener('click', e => {
    if (e.target === wrap) closeCatalogFilterPopup();
  });
  document.body.appendChild(wrap);
}

function setProductPage(page) {
  productPage = Math.max(1, page);
  renderProducts();
}

async function loadPublicCatalogMeta() {
  if (!SERVER_MODE || IS_ADMIN_PAGE || publicCatalogMeta) return publicCatalogMeta;
  try {
    const res = await fetch(API_BASE + '/api/categories', { cache: 'no-store' });
    if (!res.ok) throw new Error('Kategori tidak tersedia');
    publicCatalogMeta = await res.json();
  } catch (err) {
    console.warn('Gagal memuat kategori server, memakai data browser:', err);
    publicCatalogMeta = null;
  }
  return publicCatalogMeta;
}

async function init() {
  await loadStateFromServer();

  if (!localStorage.getItem('jsa_products')) setProducts(seed);
  if (!localStorage.getItem('jsa_categories')) setCategories(defaultCategories);
  if (IS_ADMIN_PAGE) ensureDataBarangRokokTambahan();
  if (IS_ADMIN_PAGE) syncStoredProductsWithNormalization();
  if (IS_ADMIN_PAGE) syncCategoriesWithProducts();

  updateMemberAccess();
  renderMemberBox();
  const searchBox = document.getElementById('search');
  if (searchBox) {
    searchBox.oninput = () => {
      productPage = 1;
      renderProducts();
    };
  }
  renderProducts();
  renderCart();

  if (location.pathname.includes('admin')) {
    if (localStorage.getItem('jsa_admin') === '1' && (!SERVER_MODE || getAdminToken())) {
      showDash();
    } else if (SERVER_MODE) {
      localStorage.removeItem('jsa_admin');
      localStorage.removeItem('jsa_admin_token');
    }
  }
}

function formatStock(product) {
  const big = Math.floor(n(product.stockSmall) / n(product.contentPerBig || 1));
  const small = +(n(product.stockSmall) % n(product.contentPerBig || 1)).toFixed(2);
  const bigUnit = escapeHtml(product.bigUnit);
  const smallUnit = escapeHtml(product.smallUnit);

  if (big > 0 && small > 0) return `${big} ${bigUnit} ${small} ${smallUnit}`;
  if (big > 0) return `${big} ${bigUnit}`;
  return `${small} ${smallUnit}`;
}

function calcStockSmall(stockBig, stockSmall, contentPerBig) {
  return +(n(stockBig) * n(contentPerBig || 1) + n(stockSmall)).toFixed(2);
}

function splitStock(product) {
  const content = n(product.contentPerBig || 1) || 1;
  return {
    big: Math.floor(n(product.stockSmall) / content),
    small: +(n(product.stockSmall) % content).toFixed(2)
  };
}

function productCardHtml(p) {
  const imageUrl = getProductImageUrl(p);
  return `
    <div class="product">
      <img src="${escapeHtml(imageUrl)}" alt="${escapeHtml(p.name)}" onerror="this.onerror=null;this.src='${escapeHtml(autoProductImageUrl(p))}'">
      <p class="muted">${escapeHtml(p.category)} - ${escapeHtml(p.sub)} - ${escapeHtml(p.brand || getMerek(p.name))}</p>
      <h3>${escapeHtml(p.name)}</h3>
      <p class="muted">
        Stok: <b>${formatStock(p)}</b><br>
        Total: ${p.stockSmall} ${escapeHtml(p.smallUnit)}<br>
        Isi: 1 ${escapeHtml(p.bigUnit)} = ${p.contentPerBig} ${escapeHtml(p.smallUnit)}
      </p>
      <div class="price price-vertical">
        <span>${escapeHtml(getMemberPriceLabel())} kecil / ${escapeHtml(p.smallUnit)}<br><b>${rp(getMemberPrice(p, 'small'))}</b></span>
        <span>${escapeHtml(getMemberPriceLabel())} besar / ${escapeHtml(p.bigUnit)}<br><b>${rp(getMemberPrice(p, 'big'))}</b></span>
      </div>

      <label class="mini-label">Pilih penjualan</label>
      <select id="saleMode_${p.id}" onchange="updateSaleMode(${p.id})">
        <option value="small">Jual ${escapeHtml(p.smallUnit)}</option>
        <option value="big">Jual ${escapeHtml(p.bigUnit)}</option>
      </select>

      <div class="qty-add">
        <input id="qty_${p.id}" type="number" min="0.01" step="0.01" value="1">
        <span id="unitLabel_${p.id}">${escapeHtml(p.smallUnit)}</span>
      </div>

      <p class="muted" id="saleInfo_${p.id}">1 ${escapeHtml(p.smallUnit)} = ${rp(getMemberPrice(p, 'small'))}</p>
      <button class="btn full" onclick="addCart(${p.id})" ${p.stockSmall <= 0 ? 'disabled' : ''}>Tambah</button>
    </div>
  `;
}

async function renderProductsFromServer() {
  const grid = document.getElementById('productGrid');
  if (!grid) return false;

  const meta = await loadPublicCatalogMeta();
  if (!meta) return false;

  const q = (document.getElementById('search')?.value || '').toLowerCase().trim();
  const cats = meta.productCategories?.length ? meta.productCategories : ['Semua', ...(meta.categories || []).map(c => c.name)];
  const brands = meta.brands?.length ? meta.brands : ['Semua'];
  renderCatalogFilters(cats, brands);

  const seq = ++productFetchSeq;
  grid.innerHTML = '<p class="muted">Memuat produk...</p>';

  const params = new URLSearchParams({
    page: String(productPage),
    limit: String(productPerPage)
  });
  if (q) params.set('q', q);
  if (active && active !== 'Semua') params.set('category', active);
  if (activeMerek && activeMerek !== 'Semua') params.set('brand', activeMerek);

  try {
    const res = await fetch(API_BASE + '/api/products?' + params.toString(), { cache: 'no-store' });
    if (!res.ok) throw new Error('Produk tidak tersedia');
    const data = await res.json();
    if (seq !== productFetchSeq) return true;

    const products = (data.products || []).map(normalizeProduct);
    localStorage.setItem('jsa_products', JSON.stringify(products));

    const totalPages = Math.max(1, Math.ceil(n(data.total) / productPerPage));
    if (productPage > totalPages) {
      productPage = totalPages;
      return renderProductsFromServer();
    }

    grid.innerHTML = products.map(productCardHtml).join('') || '<p>Produk tidak ditemukan. Coba kata pencarian lain.</p>';

    const pager = document.getElementById('productPager');
    if (pager) {
      pager.innerHTML = n(data.total) > productPerPage ? `
        <button class="btn secondary" onclick="setProductPage(productPage - 1)" ${productPage <= 1 ? 'disabled' : ''}>Sebelumnya</button>
        <span class="hint">Halaman ${productPage} / ${totalPages} - ${n(data.total)} barang</span>
        <button class="btn secondary" onclick="setProductPage(productPage + 1)" ${productPage >= totalPages ? 'disabled' : ''}>Berikutnya</button>
      ` : '';
    }
    return true;
  } catch (err) {
    console.warn('Gagal memuat produk server, memakai data browser:', err);
    return false;
  }
}

function renderProducts() {
  const grid = document.getElementById('productGrid');
  if (!grid) return;

  if (SERVER_MODE && !IS_ADMIN_PAGE) {
    renderProductsFromServer().then(ok => {
      if (!ok) renderProductsLocal();
    });
    return;
  }

  renderProductsLocal();
}

function renderProductsLocal() {
  const grid = document.getElementById('productGrid');
  if (!grid) return;

  const q = (document.getElementById('search')?.value || '').toLowerCase().trim();
  const products = getProducts();
  const cats = ['Semua', ...new Set(products.map(p => p.category))];
  const brands = ['Semua', ...new Set(products.map(p => p.brand || getMerek(p.name)))].sort((a,b) => a === 'Semua' ? -1 : b === 'Semua' ? 1 : a.localeCompare(b));
  renderCatalogFilters(cats, brands);

  const list = products.filter(p => {
    const matchCategory = active === 'Semua'
      || p.category === active
      || (active === 'Bahan Pokok' && isBahanPokokCategory(p.category));
    const matchMerek = activeMerek === 'Semua' || (p.brand || getMerek(p.name)) === activeMerek;
    const searchText = makeSearchText(p);
    const words = q.split(/\s+/).filter(Boolean);
    const matchSearch = words.length === 0 || words.every(w => searchText.includes(w));
    return matchCategory && matchMerek && matchSearch;
  });

  const totalPages = Math.max(1, Math.ceil(list.length / productPerPage));
  if (productPage > totalPages) productPage = totalPages;
  const start = (productPage - 1) * productPerPage;
  const pageItems = list.slice(start, start + productPerPage);

  grid.innerHTML = pageItems.map(productCardHtml).join('') || '<p>Produk tidak ditemukan. Coba ketik: djarum, super 12, 76, la, mld, surya, inter.</p>';

  const pager = document.getElementById('productPager');
  if (pager) {
    pager.innerHTML = list.length > productPerPage ? `
      <button class="btn secondary" onclick="setProductPage(productPage - 1)" ${productPage <= 1 ? 'disabled' : ''}>Sebelumnya</button>
      <span class="hint">Halaman ${productPage} / ${totalPages} - ${list.length} barang</span>
      <button class="btn secondary" onclick="setProductPage(productPage + 1)" ${productPage >= totalPages ? 'disabled' : ''}>Berikutnya</button>
    ` : '';
  }

  document.getElementById('search').oninput = () => {
    productPage = 1;
    renderProducts();
  };
}


async function memberLoginFromGate(){
  const username = document.getElementById('gateMemberUsername').value.trim();
  const password = document.getElementById('gateMemberPassword').value.trim();

  let member;
  try {
    member = await authenticateMember(username, password);
  } catch (err) {
    return alert(err.message);
  }

  localStorage.setItem('jsa_member', JSON.stringify(member));

  let members = getMembers();
  const old = members.find(m => m.username === member.username);
  if (old) Object.assign(old, member);
  else members.push(member);
  setMembers(members);

  updateMemberAccess();
  renderMemberBox();
  renderProducts();
  renderCart();
}

function updateMemberAccess(){
  const member = getMember();
  const gate = document.getElementById('memberGate');
  const app = document.getElementById('memberApp');

  if(member){
    document.body.classList.remove('member-locked');
    if(gate) gate.classList.add('hidden');
    if(app) app.classList.remove('hidden');
  }else{
    document.body.classList.add('member-locked');
    if(gate) gate.classList.remove('hidden');
    if(app) app.classList.add('hidden');
  }
}


async function memberLogin() {
  const username = document.getElementById('memberUsername').value.trim();
  const password = document.getElementById('memberPassword').value.trim();

  let member;
  try {
    member = await authenticateMember(username, password);
  } catch (err) {
    return alert(err.message);
  }

  localStorage.setItem('jsa_member', JSON.stringify(member));

  let members = getMembers();
  const old = members.find(m => m.username === member.username);
  if (old) Object.assign(old, member);
  else members.push(member);
  setMembers(members);

  updateMemberAccess();
  renderMemberBox();
  renderProducts();
  renderCart();
}

function memberKeluar() {
  localStorage.removeItem('jsa_member');
  updateMemberAccess();
  renderMemberBox();
  renderCart();
}

function renderMemberBox() {
  const m = getMember();
  const login = document.getElementById('memberLoginBox');
  const info = document.getElementById('memberInfoBox');
  const buyer = document.getElementById('buyerName');
  const phone = document.getElementById('buyerPhone');
  const profile = getCheckoutProfile();

  if (!login) return;

  if (buyer) buyer.value = profile.name || '';
  if (phone) phone.value = profile.phone || '';

  if (m) {
    login.classList.add('hidden');
    info.classList.remove('hidden');
    document.getElementById('memberGreeting').textContent = 'Halo, ' + m.name + ' (' + (m.level || 'MEMBER') + ')';
  } else {
    login.classList.remove('hidden');
    info.classList.add('hidden');
  }
}


function getSelectedSalePrice(p, saleMode) {
  return getMemberPrice(p, saleMode);
}

function updateSaleMode(id) {
  const p = getProducts().find(x => String(x.id) === String(id));
  if (!p) return;

  const saleMode = document.getElementById('saleMode_' + id)?.value || 'small';
  const unitLabel = document.getElementById('unitLabel_' + id);
  const saleInfo = document.getElementById('saleInfo_' + id);
  const price = getSelectedSalePrice(p, saleMode);

  if (saleMode === 'big') {
    if (unitLabel) unitLabel.textContent = p.bigUnit;
    if (saleInfo) saleInfo.textContent = `Harga: 1 ${p.bigUnit} = ${rp(price)} | isi ${p.contentPerBig} ${p.smallUnit}`;
  } else {
    if (unitLabel) unitLabel.textContent = p.smallUnit;
    if (saleInfo) saleInfo.textContent = `Harga: 1 ${p.smallUnit} = ${rp(price)}`;
  }
}

function addCart(id) {
  const p = getProducts().find(x => String(x.id) === String(id));
  if (!p) return;

  const mode = document.getElementById('saleMode_' + id)?.value || 'small';
  const inputQty = n(document.getElementById('qty_' + id)?.value || 1);
  if (inputQty <= 0) return alert('Jumlah harus lebih dari 0');

  const qtySmall = mode === 'big' ? inputQty * p.contentPerBig : inputQty;
  const displayUnit = mode === 'big' ? p.bigUnit : p.smallUnit;
  const displayQty = inputQty;
  const displayPrice = getSelectedSalePrice(p, mode);
  const saleType = mode === 'big' ? 'besar' : 'kecil';

  const existing = cart.find(i => String(i.id) === String(id) && i.saleType === saleType);
  const currentSmallQty = cart
    .filter(i => String(i.id) === String(id))
    .reduce((s, i) => s + n(i.qtySmall), 0);

  if (currentSmallQty + qtySmall > p.stockSmall) {
    return alert('Stok tidak cukup. Stok tersedia: ' + formatStock(p));
  }

  if (existing) {
    existing.displayQty = +(n(existing.displayQty) + displayQty).toFixed(2);
    existing.qtySmall = +(n(existing.qtySmall) + qtySmall).toFixed(2);
  } else {
    cart.push({
      id: p.id,
      name: p.name,
      saleType,
      displayQty,
      displayUnit,
      displayPrice,
      qtySmall,
      smallUnit: p.smallUnit,
      priceSmall: p.priceSmall,
      costSmall: p.cost
    });
  }

  saveCart();
}


function toggleCartList(){
  cartOpen = !cartOpen;
  localStorage.setItem('jsa_cart_open', cartOpen ? '1' : '0');
  renderCart();
}

function renderCartToggle(totalItems){
  const title = document.getElementById('cartToggleTitle');
  const icon = document.getElementById('cartToggleIcon');
  const wrap = document.getElementById('cartListWrap');

  if(title) title.textContent = `Belanja (${totalItems} item)`;
  if(icon) icon.textContent = cartOpen ? 'v Sembunyikan Daftar Belanja' : '> Lihat Daftar Belanja';

  if(wrap){
    if(cartOpen) wrap.classList.remove('hidden');
    else wrap.classList.add('hidden');
  }
}


function saveCart() {
  localStorage.setItem('jsa_cart', JSON.stringify(cart));
  renderCart();
}

function refreshCartPrices() {
  const products = getProducts();
  cart.forEach(item => {
    const p = products.find(x => String(x.id) === String(item.id));
    if (!p) return;
    const saleMode = item.saleType === 'besar' ? 'big' : 'small';
    item.displayUnit = saleMode === 'big' ? p.bigUnit : p.smallUnit;
    item.displayPrice = getMemberPrice(p, saleMode);
    item.smallUnit = p.smallUnit;
    item.priceSmall = p.priceSmall;
    item.costSmall = p.cost;
  });
}

function renderCart() {
  const box = document.getElementById('cartItems');
  refreshCartPrices();
  const total = cart.reduce((s, i) => s + n(i.displayPrice) * n(i.displayQty), 0);
  const points = Math.floor(total / 100000);
  const preview = document.getElementById('cartPreview');
  const totalItems = cart.reduce((s, i) => s + n(i.displayQty || 0), 0);
  renderCartToggle(totalItems);

  if (preview) preview.textContent = rp(total);
  if (!box) return;

  box.innerHTML = cart.map((i, idx) => `
    <div class="cart-item">
      <span>
        ${escapeHtml(i.name)}<br>
        <small>${i.displayQty} ${escapeHtml(i.displayUnit)} x ${rp(i.displayPrice)}</small><br>
        <small class="muted">Diproses stok: ${i.qtySmall} ${escapeHtml(i.smallUnit)}</small>
      </span>
      <div class="cart-actions">
        <button onclick="decreaseCartItem(${idx})">-1</button>
        <button onclick="increaseCartItem(${idx})">+1</button>
        <button class="trash-btn" onclick="removeCartIndex(${idx})">Hapus</button>
      </div>
    </div>
  `).join('') || '<p class="muted">Keranjang kosong.</p>';

  document.getElementById('total').textContent = rp(total);
  document.getElementById('points').textContent = points;
}

function getCartStep(item) {
  const products = getProducts();
  const product = products.find(p => String(p.id) === String(item.id));

  return item.saleType === 'besar'
    ? n(product?.contentPerBig || item.qtySmall / item.displayQty || 1)
    : 1;
}

function increaseCartItem(index) {
  const item = cart[index];
  if (!item) return;

  const products = getProducts();
  const product = products.find(p => String(p.id) === String(item.id));
  if (!product) return alert('Produk sudah tidak ditemukan.');

  const stepSmall = getCartStep(item);
  const currentSmallQty = cart
    .filter(i => String(i.id) === String(item.id))
    .reduce((s, i) => s + n(i.qtySmall), 0);

  if (currentSmallQty + stepSmall > product.stockSmall) {
    return alert('Stok tidak cukup. Stok tersedia: ' + formatStock(product));
  }

  item.displayQty = +(n(item.displayQty) + 1).toFixed(2);
  item.qtySmall = +(n(item.qtySmall) + stepSmall).toFixed(2);

  saveCart();
}

function decreaseCartItem(index) {
  const item = cart[index];
  if (!item) return;

  const stepSmall = getCartStep(item);

  item.displayQty = +(n(item.displayQty) - 1).toFixed(2);
  item.qtySmall = +(n(item.qtySmall) - stepSmall).toFixed(2);

  if (item.displayQty <= 0 || item.qtySmall <= 0) {
    cart.splice(index, 1);
  }

  saveCart();
}

function removeCartIndex(index) {
  cart.splice(index, 1);
  saveCart();
}

function clearCart() {
  cart = [];
  saveCart();
}

function rememberBuyer(member) {
  if (!member?.phone) return;
  const members = getMembers();
  const old = members.find(m => String(m.phone) === String(member.phone) || String(m.username) === String(member.phone));
  const record = {
    id: member.phone,
    username: member.phone,
    name: member.name,
    phone: member.phone,
    level: member.level || 'MEMBER 3',
    joinedAt: old?.joinedAt || member.joinedAt || new Date().toLocaleString('id-ID')
  };
  if (old) Object.assign(old, record);
  else members.push(record);
  setMembers(members);
}


function makeReceiptPdf(order) {
  if (!window.jspdf || !window.jspdf.jsPDF) {
    alert('Library PDF belum terbaca. Pastikan internet aktif saat membuka web.');
    return;
  }

  const { jsPDF } = window.jspdf;
  const doc = new jsPDF({
    orientation: 'portrait',
    unit: 'mm',
    format: [80, 160]
  });

  let y = 8;
  const line = (txt, x = 5, size = 9, bold = false) => {
    doc.setFont('helvetica', bold ? 'bold' : 'normal');
    doc.setFontSize(size);
    doc.text(String(txt), x, y);
    y += 5;
  };

  const center = (txt, size = 10, bold = false) => {
    doc.setFont('helvetica', bold ? 'bold' : 'normal');
    doc.setFontSize(size);
    doc.text(String(txt), 40, y, { align: 'center' });
    y += 5;
  };

  const rupiah = v => 'Rp ' + Number(v || 0).toLocaleString('id-ID');

  center('JSA JAYA SAKTI ABADI', 11, true);
  center('Grosir sejak 1980', 8, false);
  center('STRUK BELANJA', 10, true);
  line('--------------------------------', 5, 9);
  line('No   : ' + order.id, 5, 8);
  line('Tgl  : ' + order.date, 5, 8);
  line('Nama : ' + order.memberName, 5, 8);
  line('HP   : ' + order.memberPhone, 5, 8);
  line('--------------------------------', 5, 9);

  order.items.forEach((item, index) => {
    const nama = `${index + 1}. ${item.name}`;
    const splitName = doc.splitTextToSize(nama, 70);
    doc.setFont('helvetica', 'normal');
    doc.setFontSize(8);
    doc.text(splitName, 5, y);
    y += splitName.length * 4;

    const qtyText = `${item.displayQty || item.qty} ${item.displayUnit || item.smallUnit || ''} x ${rupiah(item.displayPrice || item.price || 0)}`;
    line(qtyText, 7, 8);
    line('Subtotal: ' + rupiah(item.subtotal), 7, 8);
    y += 1;

    if (y > 145) {
      doc.addPage([80, 160], 'portrait');
      y = 8;
    }
  });

  line('--------------------------------', 5, 9);
  line('TOTAL : ' + rupiah(order.total), 5, 10, true);
  line('POIN  : ' + order.points, 5, 9, true);
  line('--------------------------------', 5, 9);
  center('Terima kasih sudah belanja', 8);
  center('di JSA Jaya Sakti Abadi', 8);

  doc.save('struk-jsa-' + order.id + '.pdf');
}

function downloadOrderPdf(orderId) {
  const order = getOrders().find(o => String(o.id) === String(orderId));
  if (!order) return alert('Transaksi tidak ditemukan.');
  makeReceiptPdf(order);
}

async function checkout() {
  const activeMember = getMember();
  if (!activeMember) return alert('Silakan login member dulu.');
  const profile = saveCheckoutProfile();
  if (!profile.name) return alert('Nama pembeli wajib diisi sebelum checkout.');
  if (!profile.phone) return alert('Nomor telepon pembeli wajib diisi sebelum checkout.');
  if (profile.phone.length < 8) return alert('Nomor telepon terlalu pendek.');
  const member = {
    id: profile.phone,
    username: profile.phone,
    name: profile.name,
    phone: profile.phone,
    level: activeMember.level || 'MEMBER 3',
    accountUsername: activeMember.username || activeMember.id || '',
    accountName: activeMember.name || '',
    joinedAt: new Date().toLocaleString('id-ID')
  };
  if (!cart.length) return alert('Keranjang kosong.');

  if (SERVER_MODE) {
    try {
      const res = await fetch(API_BASE + '/api/checkout', {
        method: 'POST',
        headers: apiHeaders({ 'Content-Type': 'application/json' }),
        body: JSON.stringify({ member, cart })
      });
      const result = await res.json();
      if (!res.ok || !result.ok) return alert(result.error || 'Checkout gagal.');

      syncingState = true;
      applyServerState(result.state);
      syncingState = false;

      makeReceiptPdf(result.order);
      alert('Checkout berhasil. Stok dikurangi di server lokal.');
      rememberBuyer(member);
      clearCart();
      await loadStateFromServer();
      renderProducts();
      renderStats();
      renderStockReport();
      updateStockBadge();
      return;
    } catch (err) {
      syncingState = false;
      return alert('Checkout gagal terhubung ke server lokal: ' + err.message);
    }
  }

  const products = getProducts();

  for (const item of cart) {
    const p = products.find(x => String(x.id) === String(item.id));
    const saleMode = item.saleType === 'besar' ? 'big' : 'small';
    const qtySmall = saleMode === 'big' ? n(item.displayQty) * n(p?.contentPerBig || 1) : n(item.displayQty);
    if (!p) return alert('Produk ' + item.name + ' sudah dihapus admin.');
    if (qtySmall <= 0) return alert('Jumlah ' + p.name + ' tidak valid.');
    if (qtySmall > p.stockSmall) return alert('Stok ' + p.name + ' tidak cukup. Tersedia ' + formatStock(p));
  }

  const items = cart.map(item => {
    const p = products.find(x => String(x.id) === String(item.id));
    const saleMode = item.saleType === 'besar' ? 'big' : 'small';
    const displayQty = n(item.displayQty);
    const qtySmall = saleMode === 'big' ? displayQty * n(p.contentPerBig || 1) : displayQty;
    const displayUnit = saleMode === 'big' ? p.bigUnit : p.smallUnit;
    const displayPrice = getMemberPrice(p, saleMode);
    const subtotal = displayPrice * displayQty;
    const modal = p.cost * qtySmall;

    p.stockSmall = +(n(p.stockSmall) - n(qtySmall)).toFixed(2);

    return {
      id: p.id,
      name: p.name,
      saleType: item.saleType,
      displayQty,
      displayUnit,
      displayPrice,
      qtySmall,
      smallUnit: p.smallUnit,
      priceSmall: p.priceSmall,
      costSmall: p.cost,
      subtotal,
      modal,
      profit: subtotal - modal
    };
  });

  setProducts(products);

  const total = items.reduce((s, i) => s + i.subtotal, 0);
  const modal = items.reduce((s, i) => s + i.modal, 0);
  const profit = total - modal;

  const orders = getOrders();
  const order = {
    id: Date.now(),
    date: new Date().toLocaleString('id-ID'),
    memberName: member.name,
    memberPhone: member.phone,
    memberLevel: member.level,
    items,
    total,
    modal,
    profit,
    points: Math.floor(total / 100000)
  };
  orders.unshift(order);
  setOrders(orders);
  rememberBuyer(member);

  makeReceiptPdf(order);

  alert('Checkout berhasil. Stok otomatis berkurang dan struk PDF dibuat.');
  clearCart();
  renderProducts();
}

function showAdminPage(page){
  document.querySelectorAll('.admin-page').forEach(el => el.classList.remove('active'));
  document.querySelectorAll('.tab-btn').forEach(el => el.classList.remove('active'));

  const target = document.getElementById('page-' + page);
  if(target) target.classList.add('active');

  const btns = document.querySelectorAll('.tab-btn');
  const map = {kategori:0, tambah:1, barang:2, stok:3, member:4, akun:5, backup:6};
  if(btns[map[page]]) btns[map[page]].classList.add('active');

  localStorage.setItem('jsa_admin_page', page);

  if(page === 'stok') renderStockReport();
  if(page === 'member') renderOrders();
  if(page === 'barang') renderAdmin();
  if(page === 'kategori') renderCategoryAdmin();
  if(page === 'akun') renderMembers();
}

async function login() {
  const username = byId('username')?.value || '';
  const password = byId('password')?.value || '';

  if (SERVER_MODE) {
    try {
      const res = await fetch(API_BASE + '/api/admin-login', {
        method: 'POST',
        headers: apiHeaders({ 'Content-Type': 'application/json' }),
        body: JSON.stringify({ username, password })
      });
      const result = await res.json();
      if (!res.ok || !result.ok) return alert(result.error || 'Login salah');
      if (result.token) localStorage.setItem('jsa_admin_token', result.token);
      localStorage.setItem('jsa_admin', '1');
      showDash();
      return;
    } catch (err) {
      console.warn('Login admin server gagal:', err);
      return alert('Server admin tidak terhubung. Jalankan server atau cek konfigurasi online.');
    }
  }

  if (username === 'admin' && password === 'admin123') {
    localStorage.setItem('jsa_admin', '1');
    showDash();
  } else {
    alert('Login salah');
  }
}

function logout() {
  stopAdminAutoRefresh();
  localStorage.removeItem('jsa_admin');
  localStorage.removeItem('jsa_admin_token');
  location.reload();
}

function showDash() {
  document.getElementById('loginBox')?.classList.add('hidden');
  document.getElementById('dashboard')?.classList.remove('hidden');
  renderCategoryAdmin();
  refreshCategoryOptions();
  renderAdmin();
  renderOrders();
  renderStats();
  renderStockReport();
  updateStockBadge();
  showAdminPage(localStorage.getItem('jsa_admin_page') || 'kategori');
  startAdminAutoRefresh();
  refreshAdminFromServer(true);
}

function renderCategoryAdmin() {
  const el = document.getElementById('categoryList');
  if (!el) return;
  syncCategoriesWithProducts();
  el.innerHTML = getCategories().map((c, i) => `
    <div class="cat-card">
      <b>${escapeHtml(c.name)}</b>
      <small>Sub menu: ${c.subs.join(', ')}</small>
      <button onclick="editCategory(${i})">Edit</button>
      <button onclick="deleteCategory(${i})">Hapus</button>
    </div>
  `).join('');
}

function refreshCategoryOptions() {
  const sel = document.getElementById('pCategory');
  if (!sel) return;
  syncCategoriesWithProducts();
  sel.innerHTML = getCategories().map(c => `<option value="${escapeHtml(c.name)}">${escapeHtml(c.name)}</option>`).join('');
  refreshSubOptions();
}

function refreshSubOptions() {
  const cat = document.getElementById('pCategory')?.value;
  const sub = document.getElementById('pSub');
  if (!sub) return;
  const c = getCategories().find(x => x.name === cat);
  sub.innerHTML = (c?.subs || ['Umum']).map(x => `<option value="${escapeHtml(x)}">${escapeHtml(x)}</option>`).join('');
}

function saveCategory() {
  const name = normalizeCategoryName(document.getElementById('catName').value);
  const subs = document.getElementById('subNames').value.split(',').map(x => x.trim()).filter(Boolean);
  if (!name) return alert('Nama kategori wajib diisi');

  const cats = getCategories();
  const old = cats.find(c => normalizeCategoryName(c.name).toLowerCase() === name.toLowerCase());

  if (old) {
    old.subs = [...new Set([...old.subs, ...subs])];
  } else {
    cats.push({ name, subs: subs.length ? subs : ['Umum'] });
  }

  setCategories(cats);
  byId('catName').value = '';
  byId('subNames').value = '';
  renderCategoryAdmin();
  refreshCategoryOptions();
  renderAdmin();
}

function editCategory(i) {
  const cats = getCategories();
  const c = cats[i];
  const name = prompt('Nama kategori', c.name);
  if (!name) return;
  const subs = prompt('Sub menu, pisahkan koma', c.subs.join(', '));
  c.name = normalizeCategoryName(name);
  c.subs = (subs || 'Umum').split(',').map(x => x.trim()).filter(Boolean);
  setCategories(cats);
  renderCategoryAdmin();
  refreshCategoryOptions();
  renderAdmin();
}

function deleteCategory(i) {
  if (!confirm('Hapus kategori? Produk tidak ikut terhapus.')) return;
  const cats = getCategories();
  cats.splice(i, 1);
  setCategories(cats);
  renderCategoryAdmin();
  refreshCategoryOptions();
  renderAdmin();
}

function splitGroupKey(group = '') {
  const parts = String(group || '').split(' / ');
  const category = normalizeCategoryName(parts.shift() || '');
  return {
    category,
    sub: normalizeSubName(category, parts.join(' / ') || '')
  };
}

function productMatchesGroup(product, group) {
  const target = splitGroupKey(group);
  return normalizeCategoryName(product.category) === target.category
    && normalizeSubName(product.category, product.sub) === target.sub;
}

function groupOrganizerHtml(group, index) {
  const target = splitGroupKey(group);
  return `
    <div class="group-organizer">
      <div class="group-organizer-info">
        <b>Alat sementara</b>
        <small>Pindahkan semua barang di grup ini ke kategori/sub yang dipilih.</small>
      </div>
      <label>Kategori tujuan
        <input id="moveCat_${index}" value="${escapeHtml(target.category)}" list="categoryMoveList">
      </label>
      <label>Sub tujuan
        <input id="moveSub_${index}" value="${escapeHtml(target.sub)}">
      </label>
      <button class="btn secondary" onclick="moveProductGroup('${encodeURIComponent(group)}', ${index})">Pindahkan Grup</button>
    </div>
  `;
}

function moveProductGroup(encodedGroup, index) {
  const group = decodeURIComponent(encodedGroup);
  const oldTarget = splitGroupKey(group);
  const nextCategory = normalizeCategoryName(document.getElementById('moveCat_' + index)?.value || '');
  const nextSub = normalizeSubName(nextCategory, document.getElementById('moveSub_' + index)?.value || '');

  if (!nextCategory || !nextSub) return alert('Kategori dan sub tujuan wajib diisi.');
  if (oldTarget.category === nextCategory && oldTarget.sub === nextSub) {
    return alert('Tujuan masih sama dengan grup sekarang.');
  }
  if (!confirm(`Pindahkan semua barang dari "${oldTarget.category} / ${oldTarget.sub}" ke "${nextCategory} / ${nextSub}"?`)) return;

  const products = getProducts();
  let moved = 0;
  products.forEach(product => {
    if (!productMatchesGroup(product, group)) return;
    product.category = nextCategory;
    product.sub = nextSub;
    product.brand = nextSub;
    moved += 1;
  });

  if (!moved) return alert('Tidak ada barang yang cocok untuk dipindahkan.');

  setProducts(products);
  syncCategoriesWithProducts();
  renderCategoryAdmin();
  refreshCategoryOptions();
  renderAdmin();
  renderProducts();
  renderStockReport();
  alert(`${moved} barang dipindahkan ke ${nextCategory} / ${nextSub}.`);
}

function fillMissingProductImages() {
  const products = getProducts();
  let filled = 0;
  products.forEach(product => {
    if (String(product.img || '').trim()) return;
    product.img = autoProductImageUrl(product);
    filled += 1;
  });

  if (!filled) return alert('Semua produk sudah punya URL gambar.');

  setProducts(products);
  renderAdmin();
  renderProducts();
  renderStockReport();
  alert(`${filled} produk sudah diisi URL gambar otomatis.`);
}

function renderAdmin() {
  const list = document.getElementById('adminList');
  if (!list) return;

  const q = (document.getElementById('adminSearch')?.value || '').toLowerCase().trim();
  const products = getProducts();

  let filtered = products.filter(p => {
    const text = `${p.name} ${p.brand || ''} ${p.category} ${p.sub} ${p.id}`.toLowerCase();
    return !q || q.split(/\s+/).every(w => text.includes(w));
  });

  const totalPages = Math.max(1, Math.ceil(filtered.length / adminPerPage));
  if (adminPage > totalPages) adminPage = totalPages;

  const start = (adminPage - 1) * adminPerPage;
  const pageItems = filtered.slice(start, start + adminPerPage);

  const info = document.getElementById('adminPageInfo');
  if (info) info.textContent = `Halaman ${adminPage} / ${totalPages} - ${filtered.length} barang`;

  const grouped = {};
  pageItems.forEach(p => {
    const key = `${p.category} / ${p.sub}`;
    if(!grouped[key]) grouped[key] = [];
    grouped[key].push(p);
  });

  const categoryOptions = getCategories()
    .map(c => `<option value="${escapeHtml(c.name)}"></option>`)
    .join('');

  list.innerHTML = `
    <datalist id="categoryMoveList">${categoryOptions}</datalist>
    ${Object.entries(grouped).map(([group, items], groupIndex) => {
    const isGroupOpen = !!openProductGroups[group];
    return `
      <div class="admin-cat compact-cat group-card">
        <button class="group-head" onclick="toggleProductGroup('${encodeURIComponent(group)}')">
          <span>
            <b>${escapeHtml(group)}</b>
            <small>${items.length} barang di halaman ini</small>
          </span>
          <span class="group-arrow ${isGroupOpen ? 'is-open' : ''}" aria-hidden="true"></span>
        </button>
        <div class="compact-list ${isGroupOpen ? '' : 'hidden'}">
          ${groupOrganizerHtml(group, groupIndex)}
          ${items.map(p => productAccordion(p)).join('')}
        </div>
      </div>
    `;
  }).join('') || '<p class="muted">Produk tidak ditemukan.</p>'}
  `;

  renderStats();
}

function toggleSub(encoded) {
  openSubKey = decodeURIComponent(encoded);
  localStorage.setItem('jsa_open_sub', openSubKey);
  renderAdmin();
}




function closeAllProductGroups(){
  openProductGroups = {};
  localStorage.setItem('jsa_open_product_groups', JSON.stringify(openProductGroups));
  renderAdmin();
}

function openAllProductGroups(){
  const q = (document.getElementById('adminSearch')?.value || '').toLowerCase().trim();
  const products = getProducts().filter(p => {
    const text = `${p.name} ${p.brand || ''} ${p.category} ${p.sub} ${p.id}`.toLowerCase();
    return !q || q.split(/\s+/).every(w => text.includes(w));
  });
  const start = (adminPage - 1) * adminPerPage;
  const pageItems = products.slice(start, start + adminPerPage);

  pageItems.forEach(p => {
    const group = `${p.category} / ${p.sub}`;
    openProductGroups[group] = true;
  });

  localStorage.setItem('jsa_open_product_groups', JSON.stringify(openProductGroups));
  renderAdmin();
}


function toggleProductGroup(encodedGroup) {
  const group = decodeURIComponent(encodedGroup);
  openProductGroups[group] = !openProductGroups[group];
  localStorage.setItem('jsa_open_product_groups', JSON.stringify(openProductGroups));
  renderAdmin();
}


function productAccordion(p) {
  const isOpen = String(openProductId) === String(p.id);
  const isLow = Number(p.stockSmall || 0) <= Number(p.thresholdSmall ?? 5);
  return `
    <div class="product-accordion ${isOpen ? 'open' : ''}">
      <button class="product-acc-head" onclick="toggleProductEdit(${p.id})">
        <span>
          <b>${escapeHtml(p.name)}</b>
          <small>${escapeHtml(p.brand || getMerek(p.name))} - Stok: ${formatStock(p)}</small>
        </span>
        <span class="acc-status">
          ${isLow ? '<em class="mini-warning">Menipis</em>' : ''}
          <span class="acc-arrow ${isOpen ? 'is-open' : ''}" aria-hidden="true"></span>
        </span>
      </button>
      <div class="product-acc-body ${isOpen ? '' : 'hidden'}">
        ${isOpen ? productEditor(p) : ''}
      </div>
    </div>
  `;
}

function toggleProductEdit(id) {
  if (String(openProductId) === String(id)) {
    openProductId = '';
  } else {
    openProductId = String(id);
  }
  localStorage.setItem('jsa_open_product', openProductId);
  renderAdmin();
}


function productEditor(p) {
  const s = splitStock(p);
  return `
    <div class="edit-card">
      <label>Nama<input id="name_${p.id}" value="${escapeHtml(p.name)}"></label>
      <label>Merek/Merk<input id="brand_${p.id}" value="${escapeHtml(p.brand || getMerek(p.name))}"></label>
      <label>Satuan besar<input id="bigUnit_${p.id}" value="${escapeHtml(p.bigUnit)}"></label>
      <label>Satuan kecil<input id="smallUnit_${p.id}" value="${escapeHtml(p.smallUnit)}"></label>
      <label>Isi per besar<input id="content_${p.id}" type="number" step="0.01" value="${p.contentPerBig}"></label>
      <label>Stok besar<input id="stockBig_${p.id}" type="number" step="0.01" value="${s.big}"></label>
      <label>Sisa kecil<input id="stockSmall_${p.id}" type="number" step="0.01" value="${s.small}"></label>
      <label>Modal/kecil<input id="cost_${p.id}" type="number" value="${p.cost}"></label>
      <label>Harga Jual kecil<input id="priceSmall_${p.id}" type="number" value="${p.priceSmall}"></label>
      <label>Harga Jual besar<input id="priceBig_${p.id}" type="number" value="${p.priceBig}"></label>
      <label>Member 1 kecil<input id="priceMember1Small_${p.id}" type="number" value="${p.priceMember1Small || ''}"></label>
      <label>Member 1 besar<input id="priceMember1Big_${p.id}" type="number" value="${p.priceMember1Big || ''}"></label>
      <label>Member 2 kecil<input id="priceMember2Small_${p.id}" type="number" value="${p.priceMember2Small || ''}"></label>
      <label>Member 2 besar<input id="priceMember2Big_${p.id}" type="number" value="${p.priceMember2Big || ''}"></label>
      <label>Batas Stok Menipis<input id="threshold_${p.id}" type="number" step="0.01" value="${p.thresholdSmall ?? 5}"></label>
      <label>Gambar<input id="img_${p.id}" value="${escapeHtml(p.img || '')}"></label>
      <p class="muted">Stok tampil: <b>${formatStock(p)}</b> | Total kecil: ${p.stockSmall} ${p.smallUnit} | Isi produk ini: 1 ${p.bigUnit} = ${p.contentPerBig} ${p.smallUnit}</p>
      <button class="btn" onclick="saveInlineProduct(${p.id})">Simpan</button>
      <button class="btn danger" onclick="deleteProduct(${p.id})">Hapus</button>
    </div>
  `;
}

function saveInlineProduct(id) {
  const products = getProducts();
  const p = products.find(x => String(x.id) === String(id));
  if (!p) return;

  p.name = document.getElementById('name_' + id).value;
  p.brand = document.getElementById('brand_' + id).value || getMerek(p.name);
  p.bigUnit = document.getElementById('bigUnit_' + id).value || 'karton';
  p.smallUnit = document.getElementById('smallUnit_' + id).value || 'pcs';
  p.contentPerBig = n(document.getElementById('content_' + id).value) || 1;
  p.stockSmall = calcStockSmall(
    document.getElementById('stockBig_' + id).value,
    document.getElementById('stockSmall_' + id).value,
    p.contentPerBig
  );
  p.cost = n(document.getElementById('cost_' + id).value);
  p.priceSmall = n(document.getElementById('priceSmall_' + id).value);
  p.priceBig = n(document.getElementById('priceBig_' + id).value);
  p.priceMember1Small = n(document.getElementById('priceMember1Small_' + id).value);
  p.priceMember1Big = n(document.getElementById('priceMember1Big_' + id).value);
  p.priceMember2Small = n(document.getElementById('priceMember2Small_' + id).value);
  p.priceMember2Big = n(document.getElementById('priceMember2Big_' + id).value);
  p.thresholdSmall = n(document.getElementById('threshold_' + id).value);
  p.img = document.getElementById('img_' + id).value;

  setProducts(products);
  renderAdmin();
  renderProducts();
  renderStockReport();
  alert('Produk tersimpan');
}

function saveProduct() {
  const contentPerBig = n(byId('pContentPerBig')?.value) || 1;
  const productName = byId('pName')?.value.trim();
  const product = {
    id: Date.now(),
    name: productName,
    brand: getMerek(productName),
    category: byId('pCategory')?.value,
    sub: byId('pSub')?.value,
    bigUnit: byId('pBigUnit')?.value.trim() || 'karton',
    smallUnit: byId('pSmallUnit')?.value.trim() || 'pcs',
    contentPerBig,
    stockSmall: calcStockSmall(byId('pStockBig')?.value, byId('pStockSmall')?.value, contentPerBig),
    cost: n(byId('pCost')?.value),
    priceSmall: n(byId('pPriceSmall')?.value),
    priceBig: n(byId('pPriceBig')?.value),
    priceMember1Small: n(byId('pPriceMember1Small')?.value || 0),
    priceMember1Big: n(byId('pPriceMember1Big')?.value || 0),
    priceMember2Small: n(byId('pPriceMember2Small')?.value || 0),
    priceMember2Big: n(byId('pPriceMember2Big')?.value || 0),
    thresholdSmall: n(byId('pBatas')?.value || 5),
    img: byId('pImage')?.value.trim()
  };

  if (!product.name) return alert('Nama produk wajib diisi');
  if (!product.category || !product.sub) return alert('Kategori dan jenis barang wajib dipilih');
  if (!product.priceSmall && !product.priceBig) return alert('Harga kecil atau harga besar wajib diisi');

  const products = getProducts();
  products.push(product);
  setProducts(products);

  ['pName','pBigUnit','pSmallUnit','pContentPerBig','pStockBig','pStockSmall','pCost','pPriceSmall','pPriceBig','pPriceMember1Small','pPriceMember1Big','pPriceMember2Small','pPriceMember2Big','pBatas','pImage'].forEach(id => document.getElementById(id).value = '');

  renderAdmin();
  renderProducts();
  alert('Produk berhasil ditambahkan');
  showAdminPage('barang');
}

function deleteProduct(id) {
  if (!confirm('Hapus produk ini?')) return;
  const sid = String(id);
  setProducts(getProducts().filter(p => String(p.id) !== sid));
  cart = cart.filter(i => String(i.id) !== sid);
  localStorage.setItem('jsa_cart', JSON.stringify(cart));
  renderAdmin();
  renderProducts();
  renderStockReport();
  renderCart();
  renderStockReport();
  updateStockBadge();
  showAdminPage(localStorage.getItem('jsa_admin_page') || 'kategori');
}

function renderStats() {
  const el = document.getElementById('adminStats');
  if (!el) return;

  const orders = getOrders();
  const products = getProducts();
  const members = getMembers();

  const omzet = orders.reduce((s, o) => s + n(o.total), 0);
  const modal = orders.reduce((s, o) => s + n(o.modal), 0);
  const profit = orders.reduce((s, o) => s + n(o.profit), 0);
  const stockValue = products.reduce((s, p) => s + n(p.stockSmall) * n(p.cost), 0);
  const stockRows = getStockRows ? getStockRows() : [];
  const menipis = stockRows.filter(r => r.status === 'MENIPIS').length;
  const habis = stockRows.filter(r => r.status === 'HABIS').length;

  el.innerHTML = `
    <div class="stat-card"><span>Jumlah Kas / Omzet</span><b>${rp(omzet)}</b></div>
    <div class="stat-card"><span>Modal Terjual</span><b>${rp(modal)}</b></div>
    <div class="stat-card"><span>Keuntungan</span><b>${rp(profit)}</b></div>
    <div class="stat-card"><span>Nilai Stok Modal</span><b>${rp(stockValue)}</b></div>
    <div class="stat-card"><span>Produk</span><b>${products.length}</b></div>
    <div class="stat-card"><span>Transaksi</span><b>${orders.length}</b></div>
    <div class="stat-card"><span>Member</span><b>${members.length}</b></div>
    <div class="stat-card warning-card"><span>Stok Menipis</span><b>${menipis}</b></div>
    <div class="stat-card danger-card"><span>Stok Habis</span><b>${habis}</b></div>
  `;
}

function renderMembers() {
  const el = byId('memberTable');
  if (!el) return;

  const members = getMembers();
  const orders = getOrders();
  if (!members.length) {
    el.innerHTML = '<p class="muted">Belum ada member yang masuk.</p>';
    return;
  }

  el.innerHTML = `
    <div class="table-wrap">
      <table>
        <thead>
          <tr>
            <th>Username</th>
            <th>Nama</th>
            <th>Level</th>
            <th>HP</th>
            <th>Terakhir Masuk</th>
            <th>Total Belanja</th>
            <th>Poin</th>
          </tr>
        </thead>
        <tbody>
          ${members.map(m => {
            const memberOrders = orders.filter(o => String(o.memberPhone) === String(m.phone) || String(o.memberName) === String(m.name));
            const total = memberOrders.reduce((s, o) => s + n(o.total), 0);
            const points = memberOrders.reduce((s, o) => s + n(o.points), 0);
            return `
              <tr>
                <td>${escapeHtml(m.username || m.id || '-')}</td>
                <td><b>${escapeHtml(m.name)}</b></td>
                <td><span class="level-badge">${escapeHtml(m.level || 'MEMBER')}</span></td>
                <td>${escapeHtml(m.phone || '-')}</td>
                <td>${escapeHtml(m.joinedAt || '-')}</td>
                <td>${rp(total)}</td>
                <td><b>${points}</b></td>
              </tr>
            `;
          }).join('')}
        </tbody>
      </table>
    </div>
  `;
}

function clearMembers() {
  if (!confirm('Hapus data member yang pernah masuk? Riwayat belanja tidak ikut terhapus.')) return;
  setMembers([]);
  localStorage.removeItem('jsa_member');
  renderMembers();
  renderStats();
  updateMemberAccess();
}

function exportData() {
  const payload = {
    exportedAt: new Date().toISOString(),
    products: getProducts(),
    categories: getCategories(),
    orders: getOrders(),
    members: getMembers()
  };
  const blob = new Blob([JSON.stringify(payload, null, 2)], { type: 'application/json' });
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = 'backup-jsa-' + new Date().toISOString().slice(0, 10) + '.json';
  a.click();
  URL.revokeObjectURL(url);
}

function importData(event) {
  const file = event.target.files?.[0];
  if (!file) return;

  const reader = new FileReader();
  reader.onload = () => {
    try {
      const data = JSON.parse(reader.result);
      if (!Array.isArray(data.products) || !Array.isArray(data.categories)) {
        throw new Error('Format backup tidak valid');
      }
      if (!confirm('Import backup akan mengganti data produk, kategori, transaksi, dan member. Lanjutkan?')) return;
      setProducts(data.products);
      setCategories(data.categories);
      setOrders(Array.isArray(data.orders) ? data.orders : []);
      setMembers(Array.isArray(data.members) ? data.members : []);
      cart = [];
      saveCart();
      showDash();
      alert('Backup berhasil diimport.');
    } catch (err) {
      alert('Import gagal: ' + err.message);
    } finally {
      event.target.value = '';
    }
  };
  reader.readAsText(file);
}


function getBatas(){
  return 5;
}

function saveBatas(){}

function getProductBatas(p){
  return Number(p.thresholdSmall ?? 5);
}

function stockStatus(p){
  const threshold = getProductBatas(p);
  if(Number(p.stockSmall || 0) <= 0) return 'HABIS';
  if(Number(p.stockSmall || 0) <= threshold) return 'MENIPIS';
  return 'AMAN';
}

function getStockRows(){
  return getProducts().map((p, index) => {
    const status = stockStatus(p);
    return {
      no: index + 1,
      nama: p.name,
      brand: p.brand || getMerek(p.name),
    thresholdSmall: Number(p.thresholdSmall ?? 5),
    priceMember1Small: Number(p.priceMember1Small || 0),
    priceMember1Big: Number(p.priceMember1Big || 0),
    priceMember2Small: Number(p.priceMember2Small || 0),
    priceMember2Big: Number(p.priceMember2Big || 0),
      kategori: p.category,
      sub: p.sub,
      stokTampil: formatStock(p),
      totalKecil: Number(p.stockSmall || 0),
      satuanKecil: p.smallUnit,
      threshold: getProductBatas(p),
      isiPerBesar: `1 ${p.bigUnit} = ${p.contentPerBig} ${p.smallUnit}`,
      modalKecil: Number(p.cost || 0),
      hargaKecil: Number(p.priceSmall || 0),
      hargaBesar: Number(p.priceBig || 0),
      nilaiModalStok: Number(p.stockSmall || 0) * Number(p.cost || 0),
      status
    };
  });
}

function setActiveStockTab(){
  ['Alert','Category','Merek','All'].forEach(name=>{
    const el = document.getElementById('stockTab' + name);
    if(el) el.classList.remove('active');
  });
  const map = {alert:'Alert', category:'Category', brand:'Merek', all:'All'};
  const activeEl = document.getElementById('stockTab' + map[stockView]);
  if(activeEl) activeEl.classList.add('active');
}

function renderStockReport(){
  setActiveStockTab();

  const box = document.getElementById('stockReport');
  const summary = document.getElementById('stockSummary');
  if(!box) return;

  const q = (document.getElementById('stockSearch')?.value || '').toLowerCase().trim();
  const rows = getStockRows().filter(r => {
    const text = `${r.nama} ${r.brand} ${r.kategori} ${r.sub} ${r.status}`.toLowerCase();
    return !q || q.split(/\s+/).every(w => text.includes(w));
  });

  const alertRows = rows.filter(r => r.status !== 'AMAN');
  const totalProduk = rows.length;
  const totalMenipis = rows.filter(r => r.status === 'MENIPIS').length;
  const totalHabis = rows.filter(r => r.status === 'HABIS').length;
  const nilaiStok = rows.reduce((s,r)=>s+r.nilaiModalStok,0);

  if(summary){
    summary.innerHTML = `
      <div class="mini-stat"><span>Total Produk Ditampilkan</span><b>${totalProduk}</b></div>
      <div class="mini-stat warning"><span>Stok Menipis</span><b>${totalMenipis}</b></div>
      <div class="mini-stat danger-mini"><span>Stok Habis</span><b>${totalHabis}</b></div>
      <div class="mini-stat"><span>Nilai Modal Stok</span><b>${rp(nilaiStok)}</b></div>
    `;
  }

  if(stockView === 'alert'){
    box.innerHTML = renderStockTable(alertRows, 'Barang Menipis / Habis');
    if(!alertRows.length) box.innerHTML = '<p class="muted">Tidak ada barang menipis atau habis. Semua stok aman.</p>';
  }

  if(stockView === 'category'){
    box.innerHTML = renderGroupedStock(rows, 'kategori');
  }

  if(stockView === 'brand'){
    box.innerHTML = renderGroupedStock(rows, 'brand');
  }

  if(stockView === 'all'){
    box.innerHTML = renderStockTable(rows, 'Semua Barang');
  }

  showStockAlertPopup();
}

function renderGroupedStock(rows, key){
  const groups = [...new Set(rows.map(r => r[key] || 'Lainnya'))].sort();
  if(!groups.length) return '<p class="muted">Data stok tidak ditemukan.</p>';

  return groups.map(group => {
    const items = rows.filter(r => (r[key] || 'Lainnya') === group);
    const warning = items.filter(r => r.status === 'MENIPIS').length;
    const habis = items.filter(r => r.status === 'HABIS').length;

    return `
      <div class="stock-group">
        <button class="sub-head" onclick="toggleStockGroup('${escapeHtml(group).replaceAll("'","&#039;")}')">
          <span>${escapeHtml(group)}</span>
          <b>${items.length} barang - ${warning} menipis - ${habis} habis</b>
        </button>
        <div class="sub-content hidden" data-stock-group="${escapeHtml(group)}">
          ${renderStockTable(items, group)}
        </div>
      </div>
    `;
  }).join('');
}

function toggleStockGroup(group){
  document.querySelectorAll('[data-stock-group]').forEach(el=>{
    if(el.getAttribute('data-stock-group') === group){
      el.classList.toggle('hidden');
    }
  });
}

function renderStockTable(rows, title){
  if(!rows.length) return '<p class="muted">Data stok tidak ditemukan.</p>';

  return `
    <div class="table-wrap">
      <table>
        <thead>
          <tr>
            <th>No</th>
            <th>Produk</th>
            <th>Merek</th>
            <th>Stok</th>
            <th>Total Kecil</th>
            <th>Batas</th>
            <th>Isi</th>
            <th>Modal</th>
            <th>Harga Kecil</th>
            <th>Harga Besar</th>
            <th>Status</th>
          </tr>
        </thead>
        <tbody>
          ${rows.map(r => `
            <tr class="${r.status === 'HABIS' ? 'row-danger' : r.status === 'MENIPIS' ? 'row-warning' : ''}">
              <td>${r.no}</td>
              <td><b>${escapeHtml(r.nama)}</b><br><small>${escapeHtml(r.kategori)} / ${escapeHtml(r.sub)}</small></td>
              <td>${escapeHtml(r.brand)}</td>
              <td>${escapeHtml(r.stokTampil)}</td>
              <td>${r.totalKecil} ${escapeHtml(r.satuanKecil)}</td>
              <td>${r.threshold} ${escapeHtml(r.satuanKecil)}</td>
              <td>${escapeHtml(r.isiPerBesar)}</td>
              <td>${rp(r.modalKecil)}</td>
              <td>${rp(r.hargaKecil)}</td>
              <td>${rp(r.hargaBesar)}</td>
              <td><span class="status-badge ${r.status === 'HABIS' ? 'danger' : r.status === 'MENIPIS' ? 'warning' : 'safe'}">${r.status}</span></td>
            </tr>
          `).join('')}
        </tbody>
      </table>
    </div>
  `;
}

function showStockAlertPopup(){
  // Popup stok sengaja dimatikan agar admin tidak terganggu.
  // Peringatan cukup ditampilkan lewat badge dan tabel Stok Barang.
  updateStockBadge();
}

function updateStockBadge(){
  const badge = document.getElementById('stockBadge');
  if(!badge) return;

  const count = getStockRows().filter(r => r.status !== 'AMAN').length;
  badge.textContent = count;

  if(count > 0){
    badge.classList.remove('hidden');
  } else {
    badge.classList.add('hidden');
  }
}

function closeStockAlert(){}

function exportStockExcel(){
  if(!window.XLSX){
    alert('Library Excel belum terbaca. Pastikan internet aktif saat membuka admin.');
    return;
  }

  const rows = getStockRows();
  const data = rows.map(r => ({
    No: r.no,
    Produk: r.nama,
    Merek: r.brand,
    Kategori: r.kategori,
    SubKategori: r.sub,
    StokTampil: r.stokTampil,
    TotalStokKecil: r.totalKecil,
    SatuanKecil: r.satuanKecil,
    Batas: r.threshold,
    IsiPerSatuanBesar: r.isiPerBesar,
    ModalPerKecil: r.modalKecil,
    HargaKecil: r.hargaKecil,
    HargaBesar: r.hargaBesar,
    NilaiModalStok: r.nilaiModalStok,
    Status: r.status
  }));

  const wb = XLSX.utils.book_new();
  const ws = XLSX.utils.json_to_sheet(data);
  XLSX.utils.book_append_sheet(wb, ws, 'Laporan Stok');
  XLSX.writeFile(wb, 'laporan-stok-jsa.xlsx');
}

function exportStockPdf(){
  if (!window.jspdf || !window.jspdf.jsPDF) {
    alert('Library PDF belum terbaca. Pastikan internet aktif saat membuka admin.');
    return;
  }

  const { jsPDF } = window.jspdf;
  const doc = new jsPDF('landscape', 'mm', 'a4');
  const rows = getStockRows();
  const lowRows = rows.filter(r => r.status !== 'AMAN');
  const shown = lowRows.length ? lowRows : rows;

  let y = 12;
  doc.setFont('helvetica','bold');
  doc.setFontSize(16);
  doc.text('JSA JAYA SAKTI ABADI', 14, y);
  y += 7;

  doc.setFontSize(11);
  doc.text('Daftar Stok Barang', 14, y);
  y += 6;

  doc.setFont('helvetica','normal');
  doc.setFontSize(9);
  doc.text('Batas: per produk', 14, y);
  doc.text('Tanggal: ' + new Date().toLocaleString('id-ID'), 210, y);
  y += 7;

  doc.setFont('helvetica','bold');
  doc.text('No', 14, y);
  doc.text('Produk', 24, y);
  doc.text('Merek', 92, y);
  doc.text('Stok', 130, y);
  doc.text('Total Kecil', 168, y);
  doc.text('Batas', 198, y);
  doc.text('Harga Kecil', 225, y);
  doc.text('Harga Besar', 255, y);
  doc.text('Status', 282, y);
  y += 4;
  doc.line(14, y, 287, y);
  y += 5;

  doc.setFont('helvetica','normal');
  doc.setFontSize(8);

  shown.forEach(r => {
    if(y > 190){
      doc.addPage('a4', 'landscape');
      y = 12;
    }

    if(r.status === 'MENIPIS'){
      doc.setFillColor(255, 244, 204);
      doc.rect(12, y-4, 276, 7, 'F');
    }
    if(r.status === 'HABIS'){
      doc.setFillColor(255, 220, 220);
      doc.rect(12, y-4, 276, 7, 'F');
    }

    const nama = doc.splitTextToSize(r.nama, 62)[0];
    doc.text(String(r.no), 14, y);
    doc.text(nama, 24, y);
    doc.text(String(r.brand).slice(0,18), 92, y);
    doc.text(String(r.stokTampil).slice(0,20), 130, y);
    doc.text(`${r.totalKecil} ${r.satuanKecil}`, 168, y);
    doc.text(`${r.threshold}`, 198, y);
    doc.text(rp(r.hargaKecil), 225, y);
    doc.text(rp(r.hargaBesar), 255, y);

    doc.setFont('helvetica','bold');
    doc.text(r.status, 282, y);
    doc.setFont('helvetica','normal');

    y += 7;
  });

  doc.save('laporan-stok-jsa.pdf');
}



function formatOrderItemsCompact(items){
  if(!items || !items.length) return '-';
  const first = items.slice(0, 3).map(i => `${escapeHtml(i.name)} x${i.displayQty || i.qty} ${escapeHtml(i.displayUnit || i.smallUnit || '')}`).join('<br>');
  const more = items.length > 3 ? `<br><small>+ ${items.length - 3} item lainnya</small>` : '';
  return first + more;
}

let openOrderId = localStorage.getItem('jsa_open_order') || '';

function toggleOrderDetail(orderId){
  if(String(openOrderId) === String(orderId)){
    openOrderId = '';
  }else{
    openOrderId = String(orderId);
  }
  localStorage.setItem('jsa_open_order', openOrderId);
  renderOrders();
}

function orderDetailHtml(o){
  return `
    <div class="order-detail-box">
      <h4>Detail Belanja</h4>
      <div class="table-wrap">
        <table>
          <thead>
            <tr>
              <th>Barang</th>
              <th>Qty</th>
              <th>Harga</th>
              <th>Subtotal</th>
              <th>Modal</th>
              <th>Untung</th>
            </tr>
          </thead>
          <tbody>
            ${o.items.map(i => `
              <tr>
                <td>${escapeHtml(i.name)}</td>
                <td>${i.displayQty || i.qty} ${escapeHtml(i.displayUnit || i.smallUnit || '')}<br><small>Stok: ${i.qtySmall || i.qty} ${escapeHtml(i.smallUnit || '')}</small></td>
                <td>${rp(i.displayPrice || i.price || 0)}</td>
                <td>${rp(i.subtotal)}</td>
                <td>${rp(i.modal)}</td>
                <td><b>${rp(i.profit)}</b></td>
              </tr>
            `).join('')}
          </tbody>
        </table>
      </div>
    </div>
  `;
}


function renderOrders() {
  const el = document.getElementById('orderTable');
  if (!el) return;

  const orders = getOrders();
  if (!orders.length) {
    el.innerHTML = '<p class="muted">Belum ada transaksi.</p>';
    return;
  }

  el.innerHTML = `
    <div class="table-wrap">
      <table class="order-table">
        <thead>
          <tr>
            <th>Tanggal</th>
            <th>Member</th>
            <th>Belanja</th>
            <th>Total</th>
            <th>Modal</th>
            <th>Untung</th>
            <th>Poin</th>
            <th>Aksi</th>
          </tr>
        </thead>
        <tbody>
          ${orders.map(o => {
            const isOpen = String(openOrderId) === String(o.id);
            return `
              <tr class="order-main-row">
                <td>${o.date}</td>
                <td>
                  <b>${escapeHtml(o.memberName)}</b><br>
                  <small>${escapeHtml(o.memberPhone || '')}</small><br>
                  <span class="level-badge">${escapeHtml(o.memberLevel || 'MEMBER')}</span>
                </td>
                <td>${formatOrderItemsCompact(o.items)}</td>
                <td><b>${rp(o.total)}</b></td>
                <td>${rp(o.modal)}</td>
                <td><b>${rp(o.profit)}</b></td>
                <td>${o.points}</td>
                <td>
                  <button class="small-btn" onclick="toggleOrderDetail(${o.id})">${isOpen ? 'Minimize' : 'Detail'}</button>
                  <button class="small-btn" onclick="downloadOrderPdf(${o.id})">PDF</button>
                </td>
              </tr>
              <tr class="order-detail-row ${isOpen ? '' : 'hidden'}">
                <td colspan="8">${orderDetailHtml(o)}</td>
              </tr>
            `;
          }).join('')}
        </tbody>
      </table>
    </div>
  `;
}


function clearOrders() {
  if (!confirm('Hapus semua transaksi?')) return;
  setOrders([]);
  renderOrders();
  renderStats();
  renderStockReport();
  renderStockReport();
  updateStockBadge();
  showAdminPage(localStorage.getItem('jsa_admin_page') || 'kategori');
}

async function resetAllData() {
  if (!confirm('Reset semua data?')) return;
  localStorage.setItem('jsa_products', JSON.stringify(seed.map(normalizeProduct)));
  localStorage.setItem('jsa_categories', JSON.stringify(defaultCategories));
  localStorage.setItem('jsa_orders', '[]');
  localStorage.setItem('jsa_members', '[]');
  localStorage.removeItem('jsa_cart');
  localStorage.removeItem('jsa_member');
  localStorage.removeItem('jsa_open_sub');
  if (SERVER_MODE) await saveStateToServer(currentState());
  location.reload();
}

function escapeHtml(str) {
  return String(str || '')
    .replaceAll('&','&amp;')
    .replaceAll('<','&lt;')
    .replaceAll('>','&gt;')
    .replaceAll('"','&quot;')
    .replaceAll("'",'&#039;');
}

init();

